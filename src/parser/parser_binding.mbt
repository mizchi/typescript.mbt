///|
/// The words ES reserves only in STRICT mode, minus the two this parser
/// already spells as their own token kinds (`let` and `yield`, handled in
/// their own arms below) and minus `eval` / `arguments`, which are not
/// reserved words at all — they are ordinary identifiers strict mode
/// forbids as a binding target, and the caller tests them separately.
///
/// TS1213 ("identifier expected — `X` is a reserved word in strict mode").
/// The rule was already here for `let` and `yield`, at two of the nine
/// spellings the language has, which is why `class C { constructor(static)
/// {} }` parsed clean: a class body is automatically strict
/// (`parser_class.mbt` sets `in_strict`), and `static` arrives through the
/// plain `Ident(_)` arm next to `eval` / `arguments`.
fn is_strict_reserved_word(name : String) -> Bool {
  name == "implements" ||
  name == "interface" ||
  name == "package" ||
  name == "private" ||
  name == "protected" ||
  name == "public" ||
  name == "static"
}

///|
// /
fn Parser::parse_binding_ident(self : Parser) -> String raise ParseError {
  match self.peek().kind {
    Ident(_) =>
      match self.advance().kind {
        Ident(n) => {
          // In strict mode, `eval` and `arguments` cannot be used as binding
          // identifiers (TS1100 family). Recorded -- not raised -- so the
          // file still parses and the checker reports the diagnostic.
          if (self.in_strict || self.directive_strict) &&
            (n == "eval" || n == "arguments" || is_strict_reserved_word(n)) {
            self.strict_mode_misuses.push(n)
          }
          n
        }
        _ => "<param>"
      }
    Yield => {
      let _ = self.advance()
      // TS1212: `yield` cannot be used as a binding identifier in strict
      // mode (class bodies, "use strict" prologues, @alwaysStrict runs).
      // The ES2015+-target reservation lives in `parse_binding_pattern`:
      // this parser is also used by lenient name positions (enum members,
      // import specifiers) where keywords stay legal.
      if self.in_strict || self.directive_strict {
        self.strict_mode_misuses.push("yield")
      }
      "yield"
    }
    NumberType => {
      let _ = self.advance()
      "number"
    }
    BooleanType => {
      let _ = self.advance()
      "boolean"
    }
    StringType => {
      let _ = self.advance()
      "string"
    }
    VoidType => {
      let _ = self.advance()
      "void"
    }
    IntType => {
      let _ = self.advance()
      "int"
    }
    Type => {
      let _ = self.advance()
      "type"
    }
    Function => {
      let _ = self.advance()
      "function"
    }
    Class => {
      let _ = self.advance()
      "class"
    }
    Interface => {
      let _ = self.advance()
      "interface"
    }
    New => {
      let _ = self.advance()
      "new"
    }
    Let => {
      let _ = self.advance()
      // TS1212: same strict-mode reservation as `yield` above (the
      // ES2015+-target rule lives in `parse_binding_pattern`).
      if self.in_strict || self.directive_strict {
        self.strict_mode_misuses.push("let")
      }
      "let"
    }
    Var => {
      let _ = self.advance()
      "var"
    }
    Const => {
      let _ = self.advance()
      "const"
    }
    Switch => {
      let _ = self.advance()
      "switch"
    }
    Case => {
      let _ = self.advance()
      "case"
    }
    If => {
      let _ = self.advance()
      "if"
    }
    Else => {
      let _ = self.advance()
      "else"
    }
    While => {
      let _ = self.advance()
      "while"
    }
    Do => {
      let _ = self.advance()
      "do"
    }
    For => {
      let _ = self.advance()
      "for"
    }
    Return => {
      let _ = self.advance()
      "return"
    }
    Break => {
      let _ = self.advance()
      "break"
    }
    Continue => {
      let _ = self.advance()
      "continue"
    }
    Default => {
      let _ = self.advance()
      "default"
    }
    With => {
      let _ = self.advance()
      "with"
    }
    Debugger => {
      let _ = self.advance()
      "debugger"
    }
    Try => {
      let _ = self.advance()
      "try"
    }
    Catch => {
      let _ = self.advance()
      "catch"
    }
    Finally => {
      let _ = self.advance()
      "finally"
    }
    Throw => {
      let _ = self.advance()
      "throw"
    }
    Import => {
      let _ = self.advance()
      "import"
    }
    Export => {
      let _ = self.advance()
      "export"
    }
    Declare => {
      let _ = self.advance()
      "declare"
    }
    From => {
      let _ = self.advance()
      "from"
    }
    Delete => {
      let _ = self.advance()
      "delete"
    }
    As => {
      let _ = self.advance()
      "as"
    }
    Of => {
      let _ = self.advance()
      "of"
    }
    In => {
      let _ = self.advance()
      "in"
    }
    Extends => {
      let _ = self.advance()
      "extends"
    }
    Typeof => {
      let _ = self.advance()
      "typeof"
    }
    Instanceof => {
      let _ = self.advance()
      "instanceof"
    }
    Null => {
      let _ = self.advance()
      "null"
    }
    Bool(true) => {
      let _ = self.advance()
      "true"
    }
    Bool(false) => {
      let _ = self.advance()
      "false"
    }
    k => raise ParseError("Expected identifier, got \{k}")
  }
}

///|
fn Parser::binding_first_name(
  self : Parser,
  binding : @ast.TsBinding,
) -> String {
  match binding {
    @ast.TsBinding::Ident(n) => n
    @ast.TsBinding::Array(arr) => {
      for item in arr.items {
        match item {
          Some(elem) => {
            let name = self.binding_first_name(elem.binding)
            if name != "<destruct>" {
              return name
            }
          }
          None => ()
        }
      }
      match arr.rest {
        Some(rest) => self.binding_first_name(rest)
        None => "<destruct>"
      }
    }
    @ast.TsBinding::Object(obj) => {
      for prop in obj.props {
        let name = self.binding_first_name(prop.binding)
        if name != "<destruct>" {
          return name
        }
      }
      match obj.rest {
        Some(name) => name
        None => "<destruct>"
      }
    }
    @ast.TsBinding::Target(_) => "<destruct>"
  }
}

///|
/// TS2451 ("Cannot redeclare block-scoped variable 'X'") for two elements
/// of ONE destructuring pattern that bind the same name —
/// `let { foo, foo }`, `const { a, b: a }`, `let [v, v]`.
///
/// `is_block_scoped` is required and has no default: `var [a, a] = …`
/// legally merges, so the caller has to say which keyword it parsed. Batch
/// CA's top-level rule made the same distinction for flat declarations and
/// deliberately let patterns fall through; this is that omission, closed at
/// the one place a pattern's whole name set is known.
///
/// `destructuringSameNames.ts` is the legal-neighbour list, written into
/// the corpus:
///   - `let { foo, foo: bar }` binds `foo` and `bar` — DISTINCT, legal;
///   - every ASSIGNMENT-pattern repeat (`({ foo, foo } = …)`,
///     `[foo, foo] = …`) is legal, because an assignment target is not a
///     declaration. Those parse as expressions and never reach a binding
///     parser, so they are excluded structurally rather than by a test.
///
/// `collect_binding_names` already flattens nested patterns and rest
/// elements, so `let { a: { x }, b: { x } }` — two different properties
/// binding one name — is caught without a second walk.
fn Parser::record_pattern_duplicate_bindings(
  self : Parser,
  binding : @ast.TsBinding,
  is_block_scoped : Bool,
) -> Unit {
  if not(is_block_scoped) {
    return
  }
  match binding {
    @ast.TsBinding::Array(_) | @ast.TsBinding::Object(_) => ()
    // A plain `let x` binds one name; repeats across STATEMENTS are batch
    // CA's rule, not this one.
    _ => return
  }
  let names : Array[String] = []
  self.collect_binding_names(binding, names)
  let seen : Map[String, Unit] = {}
  let reported : Map[String, Unit] = {}
  for n in names {
    if seen.contains(n) && not(reported.contains(n)) {
      reported[n] = ()
      self.grammar_misuses.push("cannot redeclare block-scoped variable `\{n}`")
    }
    seen[n] = ()
  }
}

///|
/// Every name BOUND anywhere inside one method body, for TS7022's
/// indirect form. A returned name that is also declared here is the
/// method's own local, not the loop variable, so the cycle does not
/// exist.
///
/// This over-approximates on purpose: a `let v` in a nested block, or a
/// nested function's parameter, shadows only within its own scope, and
/// counting it anyway skips a method that might genuinely have been
/// reportable. That loses a finding; the opposite direction is a false
/// positive on legal code, which is what `class C { next() { let v = {
/// value: 1, done: false }; return v } }` beside `for (var v of new C)`
/// would be — TS7-ACCEPTED, and invisible to the conformance gate
/// because no corpus file has the shape.
fn Parser::collect_declared_names_in_block(
  self : Parser,
  block : @ast.TsBlock,
  out : Array[String],
) -> Unit {
  fn func(f : @ast.TsFunc) -> Unit {
    for p in f.params {
      out.push(p.name)
    }
    self.collect_declared_names_in_block(f.body, out)
  }

  fn expr(e : @ast.TsExpr) -> Unit {
    match e {
      @ast.TsExpr::FuncExpr(f) => func(f)
      @ast.TsExpr::ArrowFunc(params, _, body, _) => {
        for p in params {
          out.push(p.name)
        }
        match body {
          @ast.TsArrowBody::ArrowBlock(b) =>
            self.collect_declared_names_in_block(b, out)
          @ast.TsArrowBody::ArrowExpr(inner) => expr(inner)
        }
      }
      _ => ()
    }
  }

  fn stmt(st : @ast.TsStmt) -> Unit {
    match st {
      @ast.TsStmt::Let(b, _, init)
      | @ast.TsStmt::Const(b, _, init)
      | @ast.TsStmt::Var(b, _, init) => {
        self.collect_binding_names(b, out)
        expr(init)
      }
      @ast.TsStmt::Expr(e) | @ast.TsStmt::Return(Some(e)) => expr(e)
      @ast.TsStmt::Block(b) => self.collect_declared_names_in_block(b, out)
      @ast.TsStmt::If(_, t, e) => {
        self.collect_declared_names_in_block(t, out)
        match e {
          Some(b) => self.collect_declared_names_in_block(b, out)
          None => ()
        }
      }
      @ast.TsStmt::While(_, b)
      | @ast.TsStmt::DoWhile(_, b)
      | @ast.TsStmt::With(_, b) => self.collect_declared_names_in_block(b, out)
      @ast.TsStmt::For(init, _, _, b) => {
        match init {
          Some(inner) => stmt(inner)
          None => ()
        }
        self.collect_declared_names_in_block(b, out)
      }
      @ast.TsStmt::ForOf(_, hb, _, _, b)
      | @ast.TsStmt::ForAwaitOf(_, hb, _, _, b)
      | @ast.TsStmt::ForIn(_, hb, _, _, b) => {
        self.collect_binding_names(hb, out)
        self.collect_declared_names_in_block(b, out)
      }
      @ast.TsStmt::Try(t, cb, c, f) => {
        self.collect_declared_names_in_block(t, out)
        match cb {
          Some(b) => self.collect_binding_names(b, out)
          None => ()
        }
        match c {
          Some(b) => self.collect_declared_names_in_block(b, out)
          None => ()
        }
        match f {
          Some(b) => self.collect_declared_names_in_block(b, out)
          None => ()
        }
      }
      @ast.TsStmt::Switch(_, cases) =>
        for c in cases {
          self.collect_declared_names_in_block(c.body, out)
        }
      @ast.TsStmt::Label(_, inner) => stmt(inner)
      _ => ()
    }
  }

  for st in block.stmts {
    stmt(st)
  }
}

///|
/// Bare `Var` names mentioned by the `return` expressions of one method
/// body, for TS7022's INDIRECT form. Descends into blocks, branches and
/// loops — a `return` there is still this method's — and stops at a
/// nested function body, whose `return` belongs to that function.
///
/// The mention test itself is `collect_iterable_var_names`, deliberately:
/// this is the same question ("does this expression evaluate a reference
/// to NAME") that the direct form asks, and a second walk would be the
/// applied-in-some-places family in its purest form. It does mean
/// `return (() => v)()` is a MISS — tsc reports that one, because the
/// IIFE's return type feeds back — which loses a finding rather than
/// inventing one.
fn collect_method_return_var_names(
  block : @ast.TsBlock,
  out : Array[String],
) -> Unit {
  fn stmt(st : @ast.TsStmt) -> Unit {
    match st {
      @ast.TsStmt::Return(Some(e)) => collect_iterable_var_names(e, out)
      @ast.TsStmt::Block(b) => collect_method_return_var_names(b, out)
      @ast.TsStmt::If(_, t, e) => {
        collect_method_return_var_names(t, out)
        match e {
          Some(b) => collect_method_return_var_names(b, out)
          None => ()
        }
      }
      @ast.TsStmt::While(_, b)
      | @ast.TsStmt::DoWhile(_, b)
      | @ast.TsStmt::With(_, b)
      | @ast.TsStmt::For(_, _, _, b)
      | @ast.TsStmt::ForOf(_, _, _, _, b)
      | @ast.TsStmt::ForAwaitOf(_, _, _, _, b)
      | @ast.TsStmt::ForIn(_, _, _, _, b) =>
        collect_method_return_var_names(b, out)
      @ast.TsStmt::Try(t, _, c, f) => {
        collect_method_return_var_names(t, out)
        match c {
          Some(b) => collect_method_return_var_names(b, out)
          None => ()
        }
        match f {
          Some(b) => collect_method_return_var_names(b, out)
          None => ()
        }
      }
      @ast.TsStmt::Switch(_, cases) =>
        for c in cases {
          collect_method_return_var_names(c.body, out)
        }
      @ast.TsStmt::Label(_, inner) => stmt(inner)
      _ => ()
    }
  }

  for st in block.stmts {
    stmt(st)
  }
}

///|
/// TS7022 / TS2448 at the DECLARATION site — the sibling of the `for…of`
/// head rule below, which had the shape and not this spelling, so
/// `let x = x` was silent while `for (let x of x)` was reported.
///
/// `let x = x`, `const x = [x]`, `let x = typeof x` and
/// `let x = x ? 1 : 2` are all reads inside the binding's own temporal
/// dead zone. Two codes, one condition, and each is gated on the fact it
/// actually needs:
///
///   - TS2448 ("block-scoped variable 'x' used before its declaration")
///     applies to `let` / `const` whatever the annotation says —
///     `let x: number = x` is still TS2448 — because the TDZ is about
///     time and not about types.
///   - TS7022 ("implicitly has type 'any' … referenced … in its own
///     initializer") needs the annotation to be ABSENT, since an
///     annotation breaks the inference cycle. That is the only thing
///     `var x = x` gets (a `var` has no TDZ), so the `var` arm reports
///     only when unannotated and under `noImplicitAny`.
///
/// Only a flat `Ident` binding is read. A destructuring pattern is the
/// same error in principle (`const [a] = [a]`) but its names come from a
/// walk, and skipping it loses a finding rather than inventing one.
fn Parser::record_self_referential_initializer(
  self : Parser,
  binding : @ast.TsBinding,
  init : @ast.TsExpr,
  kind : VarKind,
  annotated : Bool,
) -> Unit {
  let name = match binding {
    @ast.TsBinding::Ident(n) => n
    _ => return
  }
  // `let x;` / `var x;` with no `=` at all: the parser substitutes this
  // sentinel, and there is no initializer to be self-referential.
  if init is Var("__ts_no_init__") {
    return
  }
  let mentioned : Array[String] = []
  collect_iterable_var_names(init, mentioned)
  if not(mentioned.contains(name)) {
    return
  }
  match kind {
    VarKind::Let | VarKind::Const =>
      self.grammar_misuses.push(
        "block-scoped variable `\{name}` used before its declaration",
      )
    VarKind::Var =>
      if self.no_implicit_any && not(annotated) {
        self.grammar_misuses.push(
          "<noimplicitany>`\{name}` implicitly has type `any` because it does not have a type annotation and is referenced in its own initializer",
        )
      }
  }
}

///|
/// Two rules about a `for…of` / `for…in` HEAD binding, both keyed on the
/// loop variable's own name and needing no type inference:
///
///   - TS7022 ("'X' implicitly has type 'any' because it does not have a
///     type annotation and is referenced directly or indirectly in its own
///     initializer"): `for (var v of v)`, `for (let v of [v])`,
///     `for (let v of g(v))`. Only the DIRECT form is claimed — the head
///     binds `v` and the iterable expression evaluates a reference to `v`,
///     so the type of `v` is inferred from `v`. The INDIRECT cases in the
///     corpus (`for-of33` / `-34` / `-35`, where a class's un-annotated
///     `next()` returns the loop variable) need an inference-cycle
///     detector and abstain here. Requires no annotation on the head:
///     the annotation breaks the inference cycle, so an annotated head is
///     not TS7022 — and `var_type` alone CANNOT decide that, because an
///     absent annotation and an explicit `: any` are the same
///     `TsType::Any`. `head_annotated` is therefore threaded in from the
///     parse site. The comment that used to sit here called
///     `for (var v: any of v)` "legal", and probing it shows it is not:
///     tsc gives it TS2483 ("the left-hand side of a for...of statement
///     cannot use a type annotation") plus TS2502, two codes this rule
///     does not claim. So the old behaviour was reporting the right file
///     for the wrong reason, and abstaining costs a MISS rather than
///     keeping a false positive.
///     What counts as a reference is `collect_iterable_var_names`'
///     business, and its own header records the probe that widened it.
///
///   - TS2481 ("Cannot initialize outer scoped variable 'X' in the same
///     scope as block scoped declaration 'X'"): `for (let v of []) { var v }`.
///     A `var` in the body hoists to the enclosing function while the head's
///     `let` owns the loop scope, so the two collide. Only a `let`/`const`
///     head qualifies, and only the body's own flat `var` statements are
///     read — a nested block or function is a different scope and falls
///     through, losing a finding rather than inventing one.
fn Parser::record_for_head_binding_misuses(
  self : Parser,
  kind : @ast.TsForOfKind,
  binding : @ast.TsBinding,
  var_type : @ast.TsType,
  head_annotated : Bool,
  iterable : @ast.TsExpr,
  body : @ast.TsBlock,
) -> Unit {
  let names : Array[String] = []
  self.collect_binding_names(binding, names)
  if names.length() == 0 {
    return
  }
  // TS7022: the head's own name read in the iterable, with no annotation
  // to break the cycle.
  //
  // `no_implicit_any` is REQUIRED here, and leaving it out was a false
  // positive on four TS7-ACCEPTED files: `for (var of of of) { }` and
  // `for (var of in of) { }` (parserForOfStatement18/19 and their ES5
  // twins) bind a variable named `of` and iterate a variable named `of`,
  // which is a genuine self-reference — but those files set
  // `@strict: false`, so tsc reports nothing. Every other
  // `<noimplicitany>` recorder in the parser gates on this flag; omitting
  // it here made this the twentieth instance in the repo of a rule applied
  // at some of its sites, and the first one written by the same pass that
  // was cataloguing them.
  if self.no_implicit_any && var_type is Any && not(head_annotated) {
    let mentioned : Array[String] = []
    collect_iterable_var_names(iterable, mentioned)
    for n in names {
      if mentioned.contains(n) {
        self.grammar_misuses.push(
          "<noimplicitany>`\{n}` implicitly has type `any` because it does not have a type annotation and is referenced in its own initializer",
        )
        break
      }
    }
  }
  // TS7022, the INDIRECT form: the loop's element type comes from the
  // iterated class's iteration protocol, so an un-annotated `next()` /
  // `[Symbol.iterator]()` of that class RETURNING the loop variable is a
  // genuine inference cycle (`for-of33` / `-34` / `-35`).
  //
  // The corpus supplies its own negative controls, which is what makes
  // this testable rather than a shape match on the target files:
  // `for-of25` and `for-of26` are `-33` and `-34` with the returned name
  // changed from the loop variable `v` to an unrelated `var x: any`, and
  // both are TS7-ACCEPTED. The discriminator is the NAME.
  //
  // Restricted to a `var` head, an UNANNOTATED head, and a `new C`
  // iterable. A `let` / `const` head puts the class body outside the
  // loop's scope, so the method's `v` is undeclared and tsc gives TS2304
  // instead; an annotated head breaks the inference cycle and gets
  // TS2483 + TS2502 for the annotation itself; and a `C`-annotated
  // BINDING as the iterable (`declare const c: C; for (var v of c)`) is
  // the same error in tsc but needs the annotation resolved, which this
  // site cannot do. Each loses a finding rather than inventing one.
  if self.no_implicit_any &&
    kind is @ast.TsForOfKind::Var &&
    not(head_annotated) {
    let iter_class = match iterable {
      New(c, _) => Some(c)
      NewExpr(Var(c), _) => Some(c)
      _ => None
    }
    match iter_class {
      Some(c) =>
        match self.protocol_returns_by_class.get(c) {
          Some(returned) =>
            for n in names {
              if returned.contains(n) {
                self.grammar_misuses.push(
                  "<noimplicitany>`\{n}` implicitly has type `any` because it does not have a type annotation and is referenced indirectly in its own initializer",
                )
                break
              }
            }
          None => ()
        }
      None => ()
    }
  }
  // TS2481: a body-level `var` of the head's name.
  if kind is (@ast.TsForOfKind::Let | @ast.TsForOfKind::Const) {
    for st in body.stmts {
      match st {
        @ast.TsStmt::Var(@ast.TsBinding::Ident(vn), _, _) =>
          if names.contains(vn) {
            self.grammar_misuses.push(
              "cannot initialize outer scoped variable `\{vn}` in the same scope as block scoped declaration `\{vn}`",
            )
            break
          }
        _ => ()
      }
    }
  }
}

///|
/// Bare `Var` names an initializer expression READS, for the TS7022 /
/// TS2448 self-reference test: a binding whose own initializer mentions
/// the name being bound is read inside its own temporal dead zone.
///
/// This used to be a two-arm walk over `Var` and the type-cast wrappers,
/// with a comment claiming that "a name reached through a call or a
/// property is not the iterable itself, so widening this would claim a
/// cycle that is not one". Probed, that is wrong in one direction and
/// right in another, and the difference is not call-versus-property:
///
///   for (let v of [v])       TS7022 + TS2448
///   for (let v of [1, v])    TS7022 + TS2448
///   for (let v of g(v))      TS7022 + TS2448   (a call ARGUMENT counts)
///   for (let v of [...xs, v]) TS7022 + TS2448
///   for (let v of o.v)       legal   (a property NAME is not a reference)
///   for (let v of [o2.v])    legal
///   for (let v of [() => v]) legal   (the body runs later)
///
/// So ANY evaluated reference to the name counts, and the two exemptions
/// are a property name — which the AST stores as a `String`, so it can
/// never be reached as a `Var` — and a nested function / arrow / class
/// BODY, where the read happens after the binding is initialized. That
/// is why this walk descends everywhere EXCEPT those bodies. Same rule
/// and same walk for a plain `let x = x`, which is the sibling site.
///
/// An expression form with no arm here loses a finding and cannot invent
/// one.
fn collect_iterable_var_names(e : @ast.TsExpr, out : Array[String]) -> Unit {
  match e {
    Var(n) => out.push(n)
    As(inner, _)
    | Satisfies(inner, _)
    | NonNull(inner)
    | PureCall(inner)
    | Spread(inner)
    | UnaryOp(_, inner)
    | Await(inner)
    | YieldStar(inner)
    | TypeArgs(_, inner)
    | OptionalChain(inner)
    | PropAccess(inner, _) => collect_iterable_var_names(inner, out)
    BinOp(_, l, r) | Seq(l, r) | IndexAccess(l, r) => {
      collect_iterable_var_names(l, out)
      collect_iterable_var_names(r, out)
    }
    Cond(c, t, f) => {
      collect_iterable_var_names(c, out)
      collect_iterable_var_names(t, out)
      collect_iterable_var_names(f, out)
    }
    Call(n, args) => {
      out.push(n)
      for a in args {
        collect_iterable_var_names(a, out)
      }
    }
    New(_, args) =>
      for a in args {
        collect_iterable_var_names(a, out)
      }
    CallExpr(callee, args) | NewExpr(callee, args) => {
      collect_iterable_var_names(callee, out)
      for a in args {
        collect_iterable_var_names(a, out)
      }
    }
    MethodCall(recv, _, args) => {
      collect_iterable_var_names(recv, out)
      for a in args {
        collect_iterable_var_names(a, out)
      }
    }
    ArrayLit(elems) =>
      for el in elems {
        collect_iterable_var_names(el, out)
      }
    // An object literal's KEY is a `String` in this AST, so only the
    // value can be a reference — which is also exactly right, since
    // `{ x: 1 }` is legal and the shorthand `{ x }` arrives as
    // `("x", Var("x"))` and is not.
    ObjectLit(entries) =>
      for kv in entries {
        collect_iterable_var_names(kv.1, out)
      }
    Yield(Some(inner)) => collect_iterable_var_names(inner, out)
    // Stop: a nested function / arrow / class body reads the binding
    // AFTER it is initialized, so `let x = function () { return x }` and
    // `for (let v of [() => v])` are both legal.
    _ => ()
  }
}

///|
/// Collect all bound names from a binding pattern
fn Parser::collect_binding_names(
  self : Parser,
  binding : @ast.TsBinding,
  names : Array[String],
) -> Unit {
  match binding {
    @ast.TsBinding::Ident(n) => names.push(n)
    @ast.TsBinding::Array(arr) => {
      for item in arr.items {
        match item {
          Some(elem) => self.collect_binding_names(elem.binding, names)
          None => ()
        }
      }
      match arr.rest {
        Some(rest) => self.collect_binding_names(rest, names)
        None => ()
      }
    }
    @ast.TsBinding::Object(obj) => {
      for prop in obj.props {
        self.collect_binding_names(prop.binding, names)
      }
      match obj.rest {
        Some(name) => names.push(name)
        None => ()
      }
    }
    @ast.TsBinding::Target(_) => ()
  }
}

///|
fn Parser::parse_binding_element(
  self : Parser,
) -> @ast.TsBindingElem raise ParseError {
  let binding = self.parse_binding_pattern()
  let default : @ast.TsExpr? = if self.match_(Eq) {
    Some(self.parse_assignment())
  } else {
    None
  }
  { binding, default }
}

///|
fn Parser::parse_binding_array(
  self : Parser,
) -> @ast.TsBinding raise ParseError {
  let _ = self.expect(LBracket)
  let items : Array[@ast.TsBindingElem?] = []
  let mut rest : @ast.TsBinding? = None
  while !self.check(RBracket) {
    if self.match_(Ellipsis) {
      let rest_binding = self.parse_binding_pattern()
      rest = Some(rest_binding)
      // TS1186, binding-declaration spelling: `var [...z = a] = a`. The
      // initializer is consumed so the rest of the pattern still parses —
      // this arm never handled `Eq`, so leaving it would turn a recorded
      // diagnostic into a parse failure on the closing bracket. The
      // destructuring-ASSIGNMENT spelling (`[...x = a] = a`) is the second
      // site and never reaches any pattern parser; see
      // `record_rest_element_initializer`.
      if self.check(Eq) {
        let _ = self.advance()
        let _ = self.parse_assignment()
        self.grammar_misuses.push("a rest element cannot have an initializer")
      }
      if self.match_(Comma) {
        // TS1013: a rest element may not be followed by a trailing comma.
        // No legal spelling produces it — `const [e,] = xs` (a trailing
        // comma after a NON-rest element) is fine and does not reach this
        // arm, which is the neighbour that had to be probed. Unlike the
        // parameter-list form (`declare function f(...a,)`, which tsc
        // ACCEPTS in an ambient declaration), the binding-pattern form has
        // no ambient exemption.
        self.record_suppressible_grammar_misuse(
          "a rest element may not have a trailing comma",
        )
      }
      break
    }
    if self.match_(Comma) {
      items.push(None)
      continue
    }
    items.push(Some(self.parse_binding_element()))
    if self.match_(Comma) {
      if self.check(RBracket) {
        break
      }
      continue
    } else {
      break
    }
  }
  let _ = self.expect(RBracket)
  @ast.TsBinding::Array({ items, rest })
}

///|
fn Parser::parse_object_binding_key(self : Parser) -> String raise ParseError {
  match self.advance().kind {
    Ident(n) => n
    Yield => "yield"
    Str(s) => s
    Number(n) => "\{n}"
    Int(n) => "\{n}"
    Return => "return"
    If => "if"
    Else => "else"
    For => "for"
    While => "while"
    Break => "break"
    Continue => "continue"
    Function => "function"
    Let => "let"
    Const => "const"
    Var => "var"
    New => "new"
    Typeof => "typeof"
    Of => "of"
    In => "in"
    Do => "do"
    Try => "try"
    Switch => "switch"
    Case => "case"
    Default => "default"
    With => "with"
    Debugger => "debugger"
    Import => "import"
    Export => "export"
    From => "from"
    As => "as"
    Catch => "catch"
    Finally => "finally"
    Throw => "throw"
    Delete => "delete"
    Declare => "declare"
    Interface => "interface"
    Class => "class"
    Instanceof => "instanceof"
    NumberType => "number"
    BooleanType => "boolean"
    StringType => "string"
    VoidType => "void"
    IntType => "int"
    Type => "type"
    Extends => "extends"
    Null => "null"
    // Boolean literals are valid as object-destructure keys
    // (e.g. `const { true: a, false: b } = ...`).
    Bool(true) => "true"
    Bool(false) => "false"
    k => raise ParseError("Expected object binding key, got \{k}")
  }
}

///|
fn Parser::parse_binding_object(
  self : Parser,
) -> @ast.TsBinding raise ParseError {
  let _ = self.expect(LBrace)
  let props : Array[@ast.TsObjectBindingProp] = []
  let mut rest : String? = None
  if !self.check(RBrace) {
    while true {
      if self.match_(Ellipsis) {
        let name = self.parse_binding_ident()
        rest = Some(name)
        if self.match_(Comma) {
          // TS1013, object form (`const {...b,} = o`). See the array arm.
          self.record_suppressible_grammar_misuse(
            "a rest element may not have a trailing comma",
          )
        }
        break
      }
      // A literal key token (string / number / boolean / null) can never be
      // a SHORTHAND binding — it needs the `: alias` form. Captured before
      // the key parse erases the token kind
      // (objectBindingPatternKeywordIdentifiers01/03).
      let key_is_literal_token = match self.peek().kind {
        Str(_) | Number(_) | Int(_) | Bool(_) | Null => true
        _ => false
      }
      let (key, key_expr) = if self.match_(LBracket) {
        let expr = self.parse_assignment()
        let _ = self.expect(RBracket)
        ("<computed>", Some(expr))
      } else {
        (self.parse_object_binding_key(), None)
      }
      if self.match_(Colon) {
        let elem = self.parse_binding_element()
        props.push({
          key,
          key_expr,
          binding: elem.binding,
          default: elem.default,
        })
      } else {
        match key_expr {
          Some(_) => raise ParseError("Expected Colon, got \{self.peek().kind}")
          None => {
            // Shorthand: the key IS the binding name. A hard-reserved word
            // (`var { while } = …`) or a literal key (`var { "while" } = …`)
            // is only legal with a `: alias` (TS1005).
            if key_is_literal_token {
              self.grammar_misuses.push(
                "a literal object-binding key needs a `:` alias",
              )
            } else {
              self.record_reserved_binding_name(key)
            }
            let mut default : @ast.TsExpr? = None
            if self.match_(Eq) {
              default = Some(self.parse_assignment())
            }
            props.push({
              key,
              key_expr: None,
              binding: @ast.TsBinding::Ident(key),
              default,
            })
          }
        }
      }
      if self.check(RBrace) {
        break
      }
      let _ = self.expect(Comma)
      if self.check(RBrace) {
        break
      }
    }
  }
  let _ = self.expect(RBrace)
  @ast.TsBinding::Object({ props, rest })
}

///|
fn Parser::parse_binding_pattern(
  self : Parser,
) -> @ast.TsBinding raise ParseError {
  match self.peek().kind {
    LBracket => self.parse_binding_array()
    LBrace => self.parse_binding_object()
    _ => {
      let name = self.parse_binding_ident()
      // Every caller of this parser is a genuine binding position
      // (var/let/const declarations, destructuring elements and rename
      // values, parameters, for-of bindings) -- a hard-reserved word is
      // never legal here (TS1359, `var { while: while } = …`).
      self.record_reserved_binding_name(name)
      // TS1212 at ES2015+ targets: the strict-reserved words are rejected
      // as *binding* names (`var interface`, `var let`,
      // `function f(yield = …)`) while staying legal in lenient name
      // positions (enum members, property names, import specifiers) --
      // which is why this lives here and not in `parse_binding_ident`.
      if self.target_es6_plus &&
        (name == "interface" || name == "let" || name == "yield") {
        self.strict_mode_misuses.push(name)
      }
      @ast.TsBinding::Ident(name)
    }
  }
}

///|
fn Parser::parse_assignment_target_expr(
  self : Parser,
) -> @ast.TsExpr raise ParseError {
  let expr = self.parse_ternary()
  match expr {
    Var(_) | PropAccess(_, _) | IndexAccess(_, _) => expr
    _ => raise ParseError("Invalid assignment target")
  }
}

///|
fn Parser::parse_assignment_binding_element(
  self : Parser,
) -> @ast.TsBindingElem raise ParseError {
  let binding = self.parse_assignment_binding_pattern()
  let default : @ast.TsExpr? = if self.match_(Eq) {
    Some(self.parse_assignment())
  } else {
    None
  }
  { binding, default }
}

///|
fn Parser::parse_assignment_binding_array(
  self : Parser,
) -> @ast.TsBinding raise ParseError {
  let _ = self.expect(LBracket)
  let items : Array[@ast.TsBindingElem?] = []
  let mut rest : @ast.TsBinding? = None
  while !self.check(RBracket) {
    if self.match_(Ellipsis) {
      let rest_binding = self.parse_assignment_binding_pattern()
      rest = Some(rest_binding)
      // TS1186 is not recorded here, and the reason is the one this
      // comment has always given: `[...x = a] = a` never reaches this
      // parser. `parse_assignment_binding_pattern` tries
      // `parse_assignment_target_expr` first, and for that source it
      // SUCCEEDS — the whole bracketed form parses as an expression — so
      // the array-pattern path is skipped. That is now handled where the
      // shape actually lands, in `record_rest_element_initializer`, called
      // from the object/array-literal arm of `parse_assignment`. The
      // binding-DECLARATION spelling (`var [...z = a] = a`) is the other
      // site and is checked in `parse_binding_array`.
      if self.match_(Comma) {
        // trailing comma after rest
      }
      break
    }
    if self.match_(Comma) {
      items.push(None)
      continue
    }
    items.push(Some(self.parse_assignment_binding_element()))
    if self.match_(Comma) {
      if self.check(RBracket) {
        break
      }
      continue
    } else {
      break
    }
  }
  let _ = self.expect(RBracket)
  @ast.TsBinding::Array({ items, rest })
}

///|
fn Parser::parse_assignment_binding_object(
  self : Parser,
) -> @ast.TsBinding raise ParseError {
  let _ = self.expect(LBrace)
  let props : Array[@ast.TsObjectBindingProp] = []
  let mut rest : String? = None
  if !self.check(RBrace) {
    while true {
      if self.match_(Ellipsis) {
        let name = self.parse_binding_ident()
        rest = Some(name)
        if self.match_(Comma) {
          // trailing comma
        }
        break
      }
      let (key, key_expr) = if self.match_(LBracket) {
        let expr = self.parse_assignment()
        let _ = self.expect(RBracket)
        ("<computed>", Some(expr))
      } else {
        (self.parse_object_binding_key(), None)
      }
      if self.match_(Colon) {
        let elem = self.parse_assignment_binding_element()
        props.push({
          key,
          key_expr,
          binding: elem.binding,
          default: elem.default,
        })
      } else {
        match key_expr {
          Some(_) => raise ParseError("Expected Colon, got \{self.peek().kind}")
          None => {
            let mut default : @ast.TsExpr? = None
            if self.match_(Eq) {
              default = Some(self.parse_assignment())
            }
            props.push({
              key,
              key_expr: None,
              binding: @ast.TsBinding::Ident(key),
              default,
            })
          }
        }
      }
      if self.check(RBrace) {
        break
      }
      let _ = self.expect(Comma)
      if self.check(RBrace) {
        break
      }
    }
  }
  let _ = self.expect(RBrace)
  @ast.TsBinding::Object({ props, rest })
}

///|
fn Parser::parse_assignment_binding_pattern(
  self : Parser,
) -> @ast.TsBinding raise ParseError {
  let saved_pos = self.pos
  let saved_gen = self.in_generator
  let target = Some(self.parse_assignment_target_expr()) catch { _ => None }
  match target {
    Some(expr) =>
      match expr {
        Var(name) => @ast.TsBinding::Ident(name)
        _ => @ast.TsBinding::Target(expr)
      }
    None => {
      self.pos = saved_pos
      self.in_generator = saved_gen
      match self.peek().kind {
        LBracket => self.parse_assignment_binding_array()
        LBrace => self.parse_assignment_binding_object()
        _ => {
          let name = self.parse_binding_ident()
          // TS1359: this slot is a genuine binding target (destructuring
          // element / rename value, `var { while: while } = …`), where a
          // hard-reserved word is never a legal name. `parse_binding_ident`
          // itself stays lenient because import/export specifiers and
          // property-name callers legitimately accept keywords.
          self.record_reserved_binding_name(name)
          @ast.TsBinding::Ident(name)
        }
      }
    }
  }
}

///|
/// Record a TS1359 ("Identifier expected. 'X' is a reserved word that
/// cannot be used here") when `name` is a hard-reserved word -- one that is
/// never a legal binding name in any mode. Contextual keywords (`of`,
/// `from`, `as`, `type`, `yield`, `let`, `await`, …) are excluded; their
/// mode-dependent rules live at their own parse sites.
fn Parser::record_reserved_binding_name(self : Parser, name : String) -> Unit {
  match name {
    "void"
    | "function"
    | "class"
    | "var"
    | "const"
    | "new"
    | "switch"
    | "case"
    | "if"
    | "else"
    | "while"
    | "do"
    | "for"
    | "return"
    | "break"
    | "continue"
    | "default"
    | "with"
    | "try"
    | "catch"
    | "finally"
    | "throw"
    | "delete"
    | "typeof"
    | "instanceof"
    | "in"
    // NOT `this`: a `this` first parameter (`function f(this: Foo)`) is a
    // legal this-type annotation and routes through the same binding parse.
    | "true"
    | "false"
    | "null"
    | "enum"
    | "import"
    | "export"
    | "extends"
    | "super"
    | "debugger" =>
      self.grammar_misuses.push(
        "`\{name}` is a reserved word and cannot be used as a binding name",
      )
    _ => ()
  }
}

///|
// / ...
fn Parser::match_ellipsis(self : Parser) -> Bool {
  self.match_(Ellipsis)
}
