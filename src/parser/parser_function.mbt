// ============================================
// function
// ============================================

///|
fn Parser::skip_param_decorators(self : Parser) -> Unit raise ParseError {
  while self.match_(At) {
    // TS1206: parameter decorators exist only under legacy
    // (`experimentalDecorators`) semantics; standard ES decorators have
    // no parameter form (esDecorators-classDeclaration-parameterDecorators).
    // Recorded AFTER the decorator expression is consumed: a following
    // `class` keyword means this is really a decorated CLASS EXPRESSION
    // inside parens (`(@dec class { … })`) that the arrow-params trial
    // will abandon — never a parameter decorator.
    let record_at = !self.experimental_decorators
    // `@(expr)` decorator heads (`(@((t, k, i) => {}) p: any)` —
    // legacyDecorators-contextualTypes) go straight to the balanced
    // paren skip below; identifier heads consume their member chain.
    if !self.check(LParen) {
      let _ = self.parse_binding_ident()
      while self.match_(Dot) {
        let _ = self.parse_binding_ident()
      }
      if self.check(Lt) {
        self.skip_type_args()
      }
    }
    if self.check(LParen) {
      let _ = self.advance()
      let mut paren_depth = 1
      let mut brace_depth = 0
      let mut bracket_depth = 0
      while paren_depth > 0 && !self.check(Eof) {
        match self.peek().kind {
          LParen => {
            paren_depth += 1
            let _ = self.advance()
          }
          RParen => {
            paren_depth -= 1
            let _ = self.advance()
          }
          LBrace => {
            brace_depth += 1
            let _ = self.advance()
          }
          RBrace => {
            if brace_depth > 0 {
              brace_depth -= 1
            }
            let _ = self.advance()
          }
          LBracket => {
            bracket_depth += 1
            let _ = self.advance()
          }
          RBracket => {
            if bracket_depth > 0 {
              bracket_depth -= 1
            }
            let _ = self.advance()
          }
          _ => {
            let _ = self.advance()
          }
        }
      }
    }
    // A following `@` means another stacked decorator — defer the verdict
    // to the LAST decorator in the chain, whose follower reveals whether
    // the chain precedes a class expression or a real parameter.
    if record_at && !self.check(Class) && !self.check(At) {
      self.invalid_decorator_uses.push("parameter")
    }
  }
}

///|
fn Parser::skip_param_modifiers(self : Parser) -> (Bool, Bool, String) {
  // Returns `(is_parameter_property, is_readonly, visibility)`. The first
  // is `true` when at least one TS-only parameter modifier (`public` /
  // `private` / `protected` / `readonly`) was consumed; `is_readonly` is
  // `true` when `readonly` was among them; `visibility` is the
  // accessibility keyword seen (`"private"` / `"protected"`, else
  // `"public"`). Constructor-parameter callers use these to materialize
  // the corresponding class field with the right readonly / visibility.
  let mut saw_property_modifier = false
  let mut saw_readonly = false
  let mut visibility = "public"
  let mut has_modifier = true
  // TS1029: modifier ORDER. An accessibility modifier must precede
  // `override`, so `override public foo` is an error and `public override
  // foo` is not. The rule already existed for class MEMBERS; parameter
  // properties never got it, because `skip_param_modifiers` consumed
  // `override` only to discard it (`override11`).
  let mut saw_override = false
  while has_modifier {
    has_modifier = false
    match self.peek().kind {
      Ident(name) if name == "public" ||
        name == "private" ||
        name == "protected" ||
        name == "readonly" ||
        name == "override" =>
        match self.peek_at(1).kind {
          Ident(_)
          | PrivateIdent(_)
          | LBrace
          | LBracket
          | Yield
          | NumberType
          | BooleanType
          | StringType
          | VoidType
          | IntType
          | Type
          | Of
          | In => {
            // `override` alone isn't a "parameter property"; the
            // other four (public/private/protected/readonly) are.
            if name != "override" {
              saw_property_modifier = true
              if saw_override {
                self.grammar_misuses.push(
                  "`\{name}` modifier must precede `override` modifier",
                )
              }
            } else {
              saw_override = true
            }
            if name == "readonly" {
              saw_readonly = true
            }
            if name == "private" || name == "protected" {
              visibility = name
            }
            let _ = self.advance()
            has_modifier = true
          }
          _ => ()
        }
      _ => ()
    }
  }
  (saw_property_modifier, saw_readonly, visibility)
}

///|
fn wrap_optional_ts_type(type_ : @ast.TsType) -> @ast.TsType {
  match type_ {
    @ast.TsType::Undefined | @ast.TsType::Null =>
      @ast.TsType::Union([type_, @ast.TsType::Undefined])
    @ast.TsType::Union(items) => {
      for item in items {
        match item {
          @ast.TsType::Undefined | @ast.TsType::Null => return type_
          _ => ()
        }
      }
      @ast.TsType::Union([type_, @ast.TsType::Undefined])
    }
    _ => @ast.TsType::Union([type_, @ast.TsType::Undefined])
  }
}

///|
/// TS2386: "Overload signatures must all be optional or required."
///
/// `seen` holds the `?`-ness of the FIRST method signature of each name in
/// one type body; a later signature of that name that disagrees is the
/// error. Probed against the real compiler: an object type literal, an
/// interface and a class body all report, while both-optional,
/// both-required and two different names are accepted. Only the `?`
/// matters, so no type is consulted and this can live in the parser.
///
/// Restricted to METHOD signatures on purpose. Two same-named PROPERTIES
/// (`{ m?: number; m: string }`) are a duplicate identifier — tsc gives
/// that TS2300 / TS2717, a different error which already fires — so
/// putting properties in here would report the same declaration twice.
///
/// One recorder called from every type-body parser, for the reason this
/// repo keeps recording: a rule written at the interface is a rule missing
/// from the object type literal.
///
/// `key` and `display` are separate because a class body's members are
/// keyed by static-ness as well as name (`static m` and `m` are two
/// members), while the message must name the member the way it was
/// written.
/// The map's value is `(first_optional, already_reported)`: three
/// declarations of one name where the first disagrees with the other two
/// are ONE error, so the flag keeps the message from being printed once
/// per later declaration.
fn Parser::record_overload_optionality(
  self : Parser,
  seen : Map[String, (Bool, Bool)],
  key : String,
  display : String,
  is_optional : Bool,
) -> Unit {
  match seen.get(key) {
    Some((first, reported)) =>
      if first != is_optional && not(reported) {
        seen[key] = (first, true)
        self.grammar_misuses.push(
          "overload signatures must all be optional or required: `" +
          display +
          "`",
        )
      }
    None => seen[key] = (is_optional, false)
  }
}

///|
/// TS7010 (`noImplicitAny`): a BODILESS signature with no return-type
/// annotation implicitly returns `any` — there is no body to infer from.
///
/// The rule already existed for a bodiless `function` declaration and for
/// `declare function` (see `record_implicit_any_return` and
/// `parse_declare_function`) and for nothing else, so an interface method,
/// an object-type method, a class overload signature, an `abstract`
/// member and a `declare class` member were all silent — the
/// applied-in-some-places family again. One recorder, called from every
/// member parser.
///
/// `name` is the member's own name, or the `<call>` / `<new>` sentinel,
/// which tsc gives their own codes (TS7020 / TS7013) with the signature
/// kind in place of a name. Accessors and properties are NOT here: a
/// getter with no annotation is TS7033 and a bare `p;` member is TS7008,
/// two different rules, the second of which already fires.
///
/// The test is on whether an annotation was WRITTEN, not on the resolved
/// type: `foo(n: string): any` is accepted by tsc while `foo(n: string)`
/// is the error, and the member parsers default a missing annotation to
/// `Void` or `Any` so the two are indistinguishable afterwards.
fn Parser::record_implicit_any_member_return(
  self : Parser,
  name : String,
  had_return_annotation : Bool,
) -> Unit {
  if not(self.no_implicit_any) || had_return_annotation {
    return
  }
  let what = if name == "<call>" {
    "call signature"
  } else if name == "<new>" {
    "construct signature"
  } else {
    "`" + name + "`"
  }
  self.grammar_misuses.push(
    "<noimplicitany>\{what}, which lacks a return-type annotation, implicitly has an `any` return type",
  )
}

///|
/// TS2371: "A parameter initializer is only allowed in a function or
/// constructor implementation."
///
/// The rule is about the BODY and nothing else, which is what makes it
/// decidable at parse time — every bodiless position was probed and all of
/// them report: a class-method or constructor overload signature, an
/// `abstract` member, a `declare class` member, an `interface` method, an
/// object-type member, a function TYPE (`type F = (a = 1) => void`), and a
/// bodiless `declare function` / overload signature. The legal spellings
/// are the two the message names — a function, method or constructor
/// IMPLEMENTATION — plus an arrow, whose body is the thing after the `=>`.
///
/// It lived at the two bodiless exits of `parse_function_decl` as a local
/// function, so the CLASS member path had nothing: `class C { foo(a = 4);
/// foo(a, b) {} }` parsed clean. Hoisted here so there is one recorder and
/// no site left to forget. The `interface` path (`parse_interface_body`)
/// deliberately still emits its own: it reports per PARAMETER through
/// `interface_member_modifier_misuses` with a `<param-init>` sentinel, so
/// its message can name the member and the parameter, and merging the two
/// would trade that message for a shared channel it does not need.
///
/// One message per signature, not per parameter — `f(a = 1, b = 2);` is
/// one mistake, and tsc reports each occurrence but the conformance
/// verdict is per file either way.
fn Parser::record_bodiless_param_initializers(
  self : Parser,
  params : Array[@ast.TsParam],
) -> Unit {
  for param in params {
    if param.default is Some(_) {
      self.grammar_misuses.push(
        "a parameter initializer is only allowed in a function or constructor implementation",
      )
      break
    }
  }
}

///|
// / parameter
fn Parser::parse_param(self : Parser) -> @ast.TsParam raise ParseError {
  self.skip_param_decorators()
  let (is_param_property, is_readonly_param, visibility_param) = self.skip_param_modifiers()
  let is_rest = self.match_ellipsis()
  let binding = self.parse_binding_pattern()
  let name = self.binding_first_name(binding)
  // TS1359: a *parameter* named `await` inside an async function
  // (`async function foo(await) {}`) -- the parameter binds in the async
  // body scope where `await` is reserved. The function *name* is exempt
  // (it binds in the enclosing, non-async scope: `async function await()`
  // is legal), which is why this lives here and not in
  // `parse_binding_ident`.
  if name == "await" && self.in_async {
    self.grammar_misuses.push(
      "`await` cannot be used as a parameter name in an async context",
    )
  }
  // TS1212: a parameter named `yield` inside a *generator* -- reserved in
  // generator grammar (`function * f(yield) {}`).
  if name == "yield" && self.in_generator {
    self.grammar_misuses.push(
      "`yield` cannot be used as a parameter name in a generator",
    )
  }
  // Record the name (with its `readonly`-ness and visibility) so the
  // surrounding constructor parser can synthesize `this.name = name`
  // field assigns and materialize the corresponding class field. Only
  // constructor-param parsing pushes a slot, so non-constructor params
  // are no-ops.
  if is_param_property && self.param_property_scopes.length() > 0 {
    let top = self.param_property_scopes.length() - 1
    self.param_property_scopes[top].push(
      (name, is_readonly_param, visibility_param),
    )
  } else if is_param_property {
    // A parameter property outside a constructor parameter list (free
    // function, arrow, non-constructor method, or call / construct
    // signature). TypeScript permits these modifiers only in a constructor
    // implementation, so this is a TS2369. Record it for the checker; the
    // modifier was already consumed, so parsing otherwise proceeds normally.
    self.param_property_misuses.push(name)
  }
  let is_optional = self.match_(Question)
  // type ()
  let had_annotation = self.check(Colon)
  let base_type = if had_annotation {
    let _ = self.advance()
    self.parse_type()
  } else {
    Any // type Any
  }
  // An EXPLICIT `: any` annotation is indistinguishable from a missing
  // one once stored in `TsParam.type_`, but tsc never reports TS7006
  // for it — remember the name for the noImplicitAny recorders.
  if had_annotation && base_type is Any {
    self.written_any_params[name] = ()
  }
  let type_ = if is_optional {
    wrap_optional_ts_type(base_type)
  } else {
    base_type
  }
  // check
  let default : @ast.TsExpr? = if self.match_(Eq) {
    Some(self.parse_assignment())
  } else {
    None
  }
  // TS7006 / TS7019 / TS7031 (noImplicitAny): an unannotated,
  // default-less parameter of a *function declaration* implicitly has an
  // `any` type -- declarations are never contextually typed, so the verdict
  // needs no inference. Arrows, function expressions, methods, and call
  // signatures (where a contextual type may apply) never arm the flag.
  // Recorded with the `<noimplicitany>` marker so emission stays
  // permissive-mode-only (the conformance path).
  if self.record_implicit_any_params &&
    self.no_implicit_any &&
    !had_annotation &&
    default is None &&
    name != "this" {
    match binding {
      // An *empty* pattern (`function f([]) {}` / `f({})`) binds no
      // elements, so nothing is implicitly `any` -- tsc accepts it.
      @ast.TsBinding::Object(ob) =>
        if ob.props.length() > 0 || ob.rest is Some(_) {
          self.grammar_misuses.push(
            "<noimplicitany>binding element implicitly has an `any` type",
          )
        }
      @ast.TsBinding::Array(ab) =>
        if ab.items.length() > 0 || ab.rest is Some(_) {
          self.grammar_misuses.push(
            "<noimplicitany>binding element implicitly has an `any` type",
          )
        }
      _ =>
        if is_rest {
          self.grammar_misuses.push(
            "<noimplicitany>rest parameter `\{name}` implicitly has an `any[]` type",
          )
        } else {
          self.grammar_misuses.push(
            "<noimplicitany>parameter `\{name}` implicitly has an `any` type",
          )
        }
    }
  }
  // TS1015: a parameter cannot be both optional (`x?`) and have an
  // initializer (`x = e`). The `?` is otherwise lost into the parameter
  // type below, so record the conflict here where both facts are visible.
  if is_optional && not(default is None) {
    self.param_optional_initializer_misuses.push(name)
  }
  { name, binding: Some(binding), is_rest, is_optional, type_, default }
}

///|
/// TS1064: the return type of an `async` function or method must be the
/// GLOBAL `Promise<T>`. Recorded for a return annotation TypeScript
/// definitely rejects.
///
/// A DENYLIST, for the same reason `index_signature_key_type_rejected` is
/// one, and with a sharper edge here. Every one of these was probed:
///
///   async f(): Promise<void>          legal
///   async f(): void / any / number    TS1064
///   async f(): {}                     TS1064
///   type P = Promise<void>;
///     async f(): P                    LEGAL  (the alias resolves)
///   class MyPromise<T> extends Promise<T> {}
///     async f(): MyPromise<void>      TS1064 (a subclass is not enough)
///   async f(): PromiseLike<void>      TS1064
///
/// The alias and the subclass are the SAME named-annotation node at parse
/// time, and one is legal while the other is not, so every named
/// annotation abstains. That costs the two `asyncQualifiedReturnType`
/// files (`X.MyPromise<void>` stays a MISS) and keeps the alias silent,
/// which is the right direction for a 0-FP gate. Deciding those two needs
/// the declaration table — a name declared in-file as a CLASS or INTERFACE
/// cannot be an alias — and is filed rather than built.
///
/// An async GENERATOR is exempt and the exemption is load-bearing: it
/// returns `AsyncGenerator<Y, R, N>`, not a promise, so the rule does not
/// apply at all. The corpus said so — `types.asyncGenerators.es2018.1.ts`
/// is TS7-ACCEPTED and was the round's only false positive until
/// `is_generator` came in.
fn Parser::record_async_return_type_misuse(
  self : Parser,
  is_async : Bool,
  is_generator : Bool,
  had_annotation : Bool,
  return_type : @ast.TsType,
) -> Unit {
  // TS2505: "A generator cannot have a 'void' type annotation." Same
  // question as the async rule below — does the declared return type
  // contradict the function's KIND — so it lives in the same helper rather
  // than in a second one; `is_generator` was already a parameter here, as
  // the async rule's exemption.
  //
  // Only `void` is flagged. A generator's annotation is legally
  // `Generator<…>` / `Iterator<…>` / `any` / a type parameter, and telling
  // an arbitrary wrong annotation from a legal one needs the full
  // assignability machinery; `void` is the one spelling tsc singles out
  // with its own code, and it can never be right (a generator's call
  // returns an object).
  if is_generator && had_annotation && return_type is Void {
    self.record_suppressible_grammar_misuse(
      "a generator cannot have a `void` type annotation",
    )
  }
  if !is_async || is_generator || !had_annotation {
    return
  }
  let rejected = match return_type {
    Void
    | Any
    | Unknown
    | Never
    | Null
    | Undefined
    | NonPrimitiveObject
    | Number
    | Int
    | String_
    | Boolean
    | BigInt
    | Symbol
    | Object(_)
    | Array(_)
    | Tuple(_)
    | Func(_, _) => true
    _ => false
  }
  if rejected {
    self.record_suppressible_grammar_misuse(
      "the return type of an `async` function or method must be the global `Promise<T>` type",
    )
  }
}

///|
// / parameter
fn Parser::parse_params(self : Parser) -> Array[@ast.TsParam] raise ParseError {
  let params : Array[@ast.TsParam] = []
  if !self.check(RParen) {
    params.push(self.parse_param())
    while self.match_(Comma) {
      if self.check(RParen) {
        // TS1013: a rest parameter may not have a trailing comma
        // (`function f(...args,) {}` --
        // trailingCommasInFunctionParametersAndArguments). A trailing
        // comma after an ORDINARY parameter is legal, which is why the
        // test is on the last parameter rather than on the comma, and
        // this loop is the only place that can see both facts.
        //
        // An AMBIENT signature is exempt -- the corpus file says so in a
        // comment, and tsc accepts `declare function f(...args,): void` --
        // and needs no condition here: `declare function` and
        // `declare class` route through
        // `parse_declare_signature_params`, a different function
        // entirely.
        if params.length() > 0 && params[params.length() - 1].is_rest {
          self.record_suppressible_grammar_misuse(
            "a rest parameter cannot have a trailing comma",
          )
        }
        break
      }
      params.push(self.parse_param())
    }
  }
  // TS2488: an ARRAY binding pattern's default must be iterable, so
  // `function f([...r] = null) {}` -- and `= undefined`, and `= {}` --
  // cannot destructure (restElementWithNullInitializer, whose fourth
  // function, `= []`, is the legal one). Only the three definitely-
  // non-iterable syntactic defaults are read; anything else, an
  // identifier included, abstains.
  for p in params {
    match (p.binding, p.default) {
      (Some(@ast.TsBinding::Array(_)), Some(d)) =>
        match d {
          NullLit | Var("undefined") | ObjectLit(_) =>
            self.record_suppressible_grammar_misuse(
              "an array destructuring pattern's default value must be iterable",
            )
          _ => ()
        }
      _ => ()
    }
  }
  // Parameter-list grammar rules, recorded here because this is the ONE
  // site every parameter list goes through — a method, a plain function, a
  // constructor and an arrow all land in it, so the rules cannot come to
  // hold for some spellings and not others.
  //
  // Neither has an exemption; both were probed against tsc across the
  // declaration kinds that usually need one, and an ambient
  // `declare function f(...a?: any[])` is TS1047 and
  // `declare function f(a?: number, b: number)` is TS1016 just the same.
  for i, p in params {
    // TS1047: `foo(...bar?)`.
    if p.is_rest && p.is_optional {
      self.record_suppressible_grammar_misuse(
        "a rest parameter cannot be optional",
      )
    }
    // TS1048: `foo(...bar = 0)`. The other half of the same restriction —
    // a rest parameter is always present as an array, so neither an
    // optional marker nor a default can mean anything. Written here
    // because it was NOT written here: TS1047 sat alone for as long as it
    // did, which is the twenty-first instance in this repo of one rule
    // landing at some of its spellings (parserParameterList10).
    if p.is_rest && p.default is Some(_) {
      self.record_suppressible_grammar_misuse(
        "a rest parameter cannot have an initializer",
      )
    }
    // TS1016: a REQUIRED parameter after an optional one. Required means
    // neither `?`, nor an initializer, nor a rest: `f(a = 1, b)` is legal
    // (a default does not make the parameter optional for this rule) and so
    // is `f(a?, ...b)` — both probed, and both are what a naive "not
    // optional" test would false-flag.
    if i > 0 && !p.is_optional && p.default is None && !p.is_rest {
      let mut saw_optional = false
      for j in 0..<i {
        if params[j].is_optional {
          saw_optional = true
        }
      }
      if saw_optional {
        self.record_suppressible_grammar_misuse(
          "a required parameter cannot follow an optional parameter",
        )
      }
    }
  }
  // Check for duplicate parameter names
  // In strict mode or with non-simple parameters (default, rest, destructuring),
  // duplicate names are forbidden
  let has_default = params.iter().any(fn(p) { !(p.default is None) })
  let has_rest = params.iter().any(fn(p) { p.is_rest })
  let has_destructuring = params
    .iter()
    .any(fn(p) {
      match p.binding {
        Some(@ast.TsBinding::Array(_)) | Some(@ast.TsBinding::Object(_)) => true
        _ => false
      }
    })
  let is_simple = !has_default && !has_rest && !has_destructuring
  // Collect all bound names
  let all_names : Array[String] = []
  for param in params {
    match param.binding {
      Some(binding) => self.collect_binding_names(binding, all_names)
      None => ()
    }
  }
  // Check for duplicates
  let seen : Map[String, Bool] = {}
  for name in all_names {
    if seen.contains(name) {
      // In strict mode or with non-simple params, duplicates are errors
      if self.in_strict || !is_simple {
        raise ParseError("Duplicate parameter name '\{name}' not allowed")
      }
    }
    seen[name] = true
  }
  params
}

///|
// / function
pub fn Parser::parse_function(self : Parser) -> @ast.TsFunc raise ParseError {
  // Capture the position of `function` so we can look up an
  // attached JSDoc block (for `@param {Type}` / `@returns {Type}`
  // overlay below).
  let func_kw_pos = self.peek().pos
  let _ = self.expect(Function)
  let is_generator = self.match_(Star)
  let is_async = self.in_async
  let name = self.parse_binding_ident()
  // TS1212: a *generator* cannot be named `yield` (`function * yield() {}`)
  // -- inside generator grammar `yield` is a reserved word. A plain
  // function named `yield` is additionally reserved when the conformance
  // header targets ES2015+ (`@target: es6`); without that directive it is
  // legal in non-strict code and stays silent.
  if (is_generator || self.target_es6_plus || self.in_strict) && name == "yield" {
    self.grammar_misuses.push(
      "`yield` cannot be used as the name of a function here",
    )
  }
  let (type_params, const_type_params, type_param_bounds) = self.parse_type_param_names_bounds_and_const_flags()
  let local_type_param_bound_len = self.push_local_type_param_bounds(
    type_param_bounds,
  )
  let _ = self.expect(LParen)
  let prev_record_nia = self.record_implicit_any_params
  self.record_implicit_any_params = true
  let params = self.parse_params()
  self.record_implicit_any_params = prev_record_nia
  let _ = self.expect(RParen)
  // return valuetype ()
  let had_return_annotation = self.check(Colon)
  let return_type = if had_return_annotation {
    let _ = self.advance()
    self.parse_type()
  } else {
    Any // type Any
  }
  self.restore_local_type_param_bounds(local_type_param_bound_len)
  self.record_async_return_type_misuse(
    is_async, is_generator, had_return_annotation, return_type,
  )
  // TS7010 (noImplicitAny): a *bodiless* signature (overload / ambient)
  // without a return-type annotation implicitly returns `any` -- there is
  // no body to infer from. Implementations infer and stay silent.
  fn record_implicit_any_return(p : Parser, fname : String) -> Unit {
    if p.no_implicit_any && !had_return_annotation {
      p.grammar_misuses.push(
        "<noimplicitany>function `\{fname}` lacks a return-type annotation and implicitly has an `any` return type",
      )
    }
  }

  // TS1221 / overload-signature rule: a bodiless `function*` is always an
  // error — generators are not allowed in ambient contexts, and an
  // overload signature cannot be declared as a generator. Only the
  // implementation (the decl WITH a body) may carry the `*`.
  fn record_bodiless_generator(p : Parser, fname : String) -> Unit {
    if is_generator {
      p.grammar_misuses.push(
        "a generator requires a body: `function* \{fname}` cannot be an ambient declaration or overload signature",
      )
    }
  }

  if self.check(Semicolon) {
    record_implicit_any_return(self, name)
    self.record_bodiless_param_initializers(params)
    record_bodiless_generator(self, name)
    // No body: an overload signature or an ambient declaration. Both
    // are type-only, and the caller drops the statement.
    self.last_function_bodiless = true
    let _ = self.advance()
    return {
      name,
      type_params,
      const_type_params,
      type_param_bounds,
      params,
      return_type,
      body: { stmts: [], stmt_positions: [] },
      is_generator,
      is_async,
    }
  }
  if !self.check(LBrace) {
    record_implicit_any_return(self, name)
    self.record_bodiless_param_initializers(params)
    record_bodiless_generator(self, name)
    self.last_function_bodiless = true
    return {
      name,
      type_params,
      const_type_params,
      type_param_bounds,
      params,
      return_type,
      body: { stmts: [], stmt_positions: [] },
      is_generator,
      is_async,
    }
  }
  self.last_function_bodiless = false
  // Save and set function context (new function scope resets iteration/switch/labels context)
  let prev_generator = self.in_generator
  let prev_async = self.in_async
  let prev_function = self.in_function
  let prev_iteration = self.in_iteration
  let prev_switch = self.in_switch
  // Save labels and clear for new function scope (labels don't cross function boundaries)
  let prev_labels : Array[String] = []
  for label in self.labels {
    prev_labels.push(label)
  }
  // Mirror `in_async`: the body's generator-ness follows THIS function, so
  // a plain function nested in a generator leaves the generator context
  // (`function* foo() { function bar() { yield x; } }` — the inner `yield`
  // is TS1163, YieldExpression16).
  self.in_generator = is_generator
  // The body's async-ness follows THIS function (a non-async nested
  // function leaves the async context; an `async` one enters it -- `await`
  // is reserved in its type positions).
  self.in_async = is_async
  self.in_function = true
  self.in_iteration = false
  self.in_switch = false
  // Clear labels for new function scope
  while self.labels.length() > 0 {
    let _ = self.labels.pop()
  }
  let body = self.parse_block()
  self.in_generator = prev_generator
  self.in_async = prev_async
  self.in_function = prev_function
  self.in_iteration = prev_iteration
  self.in_switch = prev_switch
  // Restore labels
  while self.labels.length() > 0 {
    let _ = self.labels.pop()
  }
  for label in prev_labels {
    self.labels.push(label)
  }
  // JSDoc type overlay: when the function lacks explicit type
  // annotations (parameters resolved to `Any` because no `: T`
  // was present, return type fell through to `Any`) AND a JSDoc
  // block precedes the `function` keyword, lift `@param {Type}` /
  // `@returns {Type}` brace contents into the AST so downstream
  // type-driven passes can reason about them.
  let final_params = apply_jsdoc_param_overlay(
    self.jsdoc_at_pos,
    func_kw_pos,
    params,
  )
  let final_return = apply_jsdoc_return_overlay(
    self.jsdoc_at_pos,
    func_kw_pos,
    return_type,
  )
  {
    name,
    type_params,
    const_type_params,
    type_param_bounds,
    params: final_params,
    return_type: final_return,
    body,
    is_generator,
    is_async,
  }
}

///|
fn apply_jsdoc_param_overlay(
  jsdoc_at_pos : Map[Int, JsdocBlock],
  pos : Int,
  params : Array[@ast.TsParam],
) -> Array[@ast.TsParam] {
  let jb = match jsdoc_at_pos.get(pos) {
    Some(b) => b
    None => return params
  }
  // Build a name -> JSDoc-type-string map.
  let by_name : Map[String, String] = {}
  for t in jb.tags {
    match t {
      Param(pname, Some(ty), _) => by_name[pname] = ty
      _ => ()
    }
  }
  if by_name.is_empty() {
    return params
  }
  let out : Array[@ast.TsParam] = []
  for p in params {
    let needs_overlay = match p.type_ {
      Any => true
      _ => false
    }
    if !needs_overlay {
      out.push(p)
      continue
    }
    match by_name.get(p.name) {
      Some(raw) =>
        match try_parse_jsdoc_type(raw) {
          Some(ty) => out.push({ ..p, type_: ty })
          None => out.push(p)
        }
      None => out.push(p)
    }
  }
  out
}

///|
fn apply_jsdoc_return_overlay(
  jsdoc_at_pos : Map[Int, JsdocBlock],
  pos : Int,
  return_type : @ast.TsType,
) -> @ast.TsType {
  match return_type {
    Any => ()
    _ => return return_type
  }
  let jb = match jsdoc_at_pos.get(pos) {
    Some(b) => b
    None => return return_type
  }
  for t in jb.tags {
    match t {
      Returns(Some(ty_str), _) =>
        match try_parse_jsdoc_type(ty_str) {
          Some(ty) => return ty
          None => ()
        }
      _ => ()
    }
  }
  return_type
}

///|
/// Try to lift a JSDoc brace-content string into a `TsType`. Many
/// JSDoc forms are already valid TS (`string`, `number`,
/// `string | number`, `Array<number>`); these parse directly. JSDoc-
/// specific forms (`function(...)`, `Object<K, V>`, `?T`, `*`) get
/// a light textual translation before the parse attempt; anything
/// that still fails resolves to `None` and the AST keeps `Any`.
fn try_parse_jsdoc_type(raw : String) -> @ast.TsType? {
  let translated = translate_jsdoc_type(raw)
  try parse_type_from_source(translated) catch {
    _ => None
  } noraise {
    ty => Some(ty)
  }
}

///|
/// Minimal JSDoc → TS textual translation. Covers the common
/// shapes; unrecognized syntax is left alone (the downstream parse
/// will simply fail and the overlay falls back to `Any`).
fn translate_jsdoc_type(raw : String) -> String {
  let s = raw.trim().to_owned()
  // `*` → `any`
  if s == "*" {
    return "any"
  }
  // `?T` → `T | null` for any type expression.
  if s.has_prefix("?") {
    let inner = s[1:].to_owned().trim().to_owned()
    if inner.length() > 0 {
      return "(\{inner} | null)"
    }
  }
  // `!T` → `T` (strip the non-null marker; TS lacks a 1:1 syntax
  // and `NonNullable<T>` is more invasive than warranted here).
  if s.has_prefix("!") {
    let inner = s[1:].to_owned().trim().to_owned()
    return inner
  }
  // `Object<K, V>` → `Record<K, V>`.
  if s.has_prefix("Object<") {
    return "Record" + s[6:].to_owned()
  }
  // `function(T1, T2): R` → `(_0: T1, _1: T2) => R`
  // `function(): R` → `() => R`
  if s.has_prefix("function(") {
    match translate_jsdoc_function_type(s) {
      Some(ts) => return ts
      None => ()
    }
  }
  s
}

///|
/// Translate `function(P1, P2): R` → `(_0: P1, _1: P2) => R`.
/// Returns `None` when the input doesn't match the expected shape.
fn translate_jsdoc_function_type(s : String) -> String? {
  // Find the closing `)` of the parameter list, respecting nesting.
  let len = s.length()
  // s starts with "function("
  let start = 9 // length of "function("
  let mut depth = 1
  let mut i = start
  while i < len && depth > 0 {
    let c = s[i]
    if c == '(' || c == '<' || c == '[' || c == '{' {
      depth = depth + 1
    } else if c == ')' || c == '>' || c == ']' || c == '}' {
      depth = depth - 1
    }
    if depth > 0 {
      i = i + 1
    }
  }
  if depth != 0 {
    return None
  }
  let params_str = s[start:i].to_owned().trim().to_owned()
  // Everything after `)` — optionally `: ReturnType`.
  let rest = s[i + 1:len].to_owned().trim().to_owned()
  let return_type = if rest.has_prefix(":") {
    rest[1:].to_owned().trim().to_owned()
  } else {
    "any"
  }
  // Split params on top-level `,`.
  let params : Array[String] = []
  if params_str.length() > 0 {
    let mut depth2 = 0
    let mut start2 = 0
    for j in 0..<params_str.length() {
      let c = params_str[j]
      if c == '(' || c == '<' || c == '[' || c == '{' {
        depth2 = depth2 + 1
      } else if c == ')' || c == '>' || c == ']' || c == '}' {
        depth2 = depth2 - 1
      } else if c == ',' && depth2 == 0 {
        params.push(params_str[start2:j].to_owned().trim().to_owned())
        start2 = j + 1
      }
    }
    params.push(params_str[start2:].to_owned().trim().to_owned())
  }
  // Build `(_0: P0, _1: P1, ...) => R`.
  let mut ts_params = ""
  for k, p in params {
    if k > 0 {
      ts_params = ts_params + ", "
    }
    ts_params = ts_params + "_\{k}: \{p}"
  }
  Some("(\{ts_params}) => \{return_type}")
}

///|
// / functionexpression()
fn Parser::parse_function_expr(self : Parser) -> @ast.TsFunc raise ParseError {
  let _ = self.expect(Function)
  let is_generator = self.match_(Star)
  let is_async = self.in_async
  let name = match self.peek().kind {
    Ident(_)
    | Yield
    | NumberType
    | BooleanType
    | StringType
    | VoidType
    | IntType
    | Type
    | Let
    | Switch
    | Case
    | Default
    | With
    | Debugger
    | Import
    | Export
    | From
    | As
    | Of
    | In => self.parse_binding_ident()
    _ => "<anon>"
  }
  // TS1212: mirror `parse_function` -- a generator expression (or any
  // function expression under an ES2015+ target / strict prologue) cannot
  // be named `yield`.
  if (is_generator || self.target_es6_plus || self.in_strict) && name == "yield" {
    self.grammar_misuses.push(
      "`yield` cannot be used as the name of a function here",
    )
  }
  // TS1359: a function *expression*'s name binds inside its own scope; in
  // an async context every identifier use of `await` is reserved, so
  // `var v = async function await() {}` (and a sync expression named
  // `await` nested in an async body) is an error. Function *declarations*
  // named `await` bind outward and stay legal outside async bodies.
  if name == "await" && self.in_async {
    self.grammar_misuses.push(
      "`await` cannot be used as the name of a function here",
    )
  }
  let (type_params, const_type_params, type_param_bounds) = self.parse_type_param_names_bounds_and_const_flags()
  let _ = self.expect(LParen)
  let params = self.parse_params()
  let _ = self.expect(RParen)
  let return_type = if self.check(Colon) {
    let _ = self.advance()
    self.parse_type()
  } else {
    Any
  }
  // Save and set function context (new function scope resets iteration/switch/labels context)
  let prev_generator = self.in_generator
  let prev_async = self.in_async
  let prev_function = self.in_function
  let prev_iteration = self.in_iteration
  let prev_switch = self.in_switch
  // Save labels and clear for new function scope (labels don't cross function boundaries)
  let prev_labels : Array[String] = []
  for label in self.labels {
    prev_labels.push(label)
  }
  // Mirror `in_async`: the body's generator-ness follows THIS function, so
  // a plain function nested in a generator leaves the generator context
  // (`function* foo() { function bar() { yield x; } }` — the inner `yield`
  // is TS1163, YieldExpression16).
  self.in_generator = is_generator
  // The body's async-ness follows THIS function (a non-async nested
  // function leaves the async context; an `async` one enters it -- `await`
  // is reserved in its type positions).
  self.in_async = is_async
  self.in_function = true
  self.in_iteration = false
  self.in_switch = false
  // Clear labels for new function scope
  while self.labels.length() > 0 {
    let _ = self.labels.pop()
  }
  let body = self.parse_block()
  self.in_generator = prev_generator
  self.in_async = prev_async
  self.in_function = prev_function
  self.in_iteration = prev_iteration
  self.in_switch = prev_switch
  // Restore labels
  while self.labels.length() > 0 {
    let _ = self.labels.pop()
  }
  for label in prev_labels {
    self.labels.push(label)
  }
  {
    name,
    type_params,
    const_type_params,
    type_param_bounds,
    params,
    return_type,
    body,
    is_generator,
    is_async,
  }
}

///|
/// Skip generic type parameters <T, U extends Foo, V = Default>
fn Parser::skip_type_params(self : Parser) -> Unit {
  if !self.check(Lt) {
    return
  }
  let _ = self.advance() // consume <
  let mut depth = 1
  while depth > 0 && !self.check(Eof) {
    match self.peek().kind {
      Lt => {
        depth += 1
        let _ = self.advance()
      }
      Gt => {
        depth -= 1
        let _ = self.advance()
      }
      GtGt => {
        // >> counts as two >
        depth -= 2
        let _ = self.advance()
      }
      GtGtGt => {
        // >>> counts as three >
        depth -= 3
        let _ = self.advance()
      }
      _ => {
        let _ = self.advance()
      }
    }
  }
}

///|
fn Parser::skip_type_param_tail_until_delim(self : Parser) -> Unit {
  let mut paren_depth = 0
  let mut bracket_depth = 0
  let mut brace_depth = 0
  let mut angle_depth = 0
  while !self.check(Eof) {
    match self.peek().kind {
      Comma if paren_depth == 0 &&
        bracket_depth == 0 &&
        brace_depth == 0 &&
        angle_depth == 0 => break
      Gt if paren_depth == 0 &&
        bracket_depth == 0 &&
        brace_depth == 0 &&
        angle_depth == 0 => break
      LParen => {
        paren_depth += 1
        let _ = self.advance()
      }
      RParen => {
        if paren_depth > 0 {
          paren_depth -= 1
        }
        let _ = self.advance()
      }
      LBracket => {
        bracket_depth += 1
        let _ = self.advance()
      }
      RBracket => {
        if bracket_depth > 0 {
          bracket_depth -= 1
        }
        let _ = self.advance()
      }
      LBrace => {
        brace_depth += 1
        let _ = self.advance()
      }
      RBrace =>
        if brace_depth > 0 {
          brace_depth -= 1
          let _ = self.advance()
        } else {
          break
        }
      Lt => {
        angle_depth += 1
        let _ = self.advance()
      }
      Gt =>
        if angle_depth > 0 {
          angle_depth -= 1
          let _ = self.advance()
        } else {
          break
        }
      GtGt =>
        if angle_depth >= 2 {
          angle_depth -= 2
          let _ = self.advance()
        } else if angle_depth == 1 {
          let tok = self.advance()
          self.pending_gt += 1
          self.pending_gt_pos = tok.pos
          angle_depth = 0
          break
        } else {
          break
        }
      GtGtGt =>
        if angle_depth >= 3 {
          angle_depth -= 3
          let _ = self.advance()
        } else if angle_depth == 2 {
          let tok = self.advance()
          self.pending_gt += 1
          self.pending_gt_pos = tok.pos
          angle_depth = 0
          break
        } else if angle_depth == 1 {
          let tok = self.advance()
          self.pending_gt += 2
          self.pending_gt_pos = tok.pos
          angle_depth = 0
          break
        } else {
          break
        }
      _ => {
        let _ = self.advance()
      }
    }
  }
}

///|
fn Parser::parse_type_param_names_and_bounds(
  self : Parser,
) -> (Array[String], Array[(String, @ast.TsType)]) {
  let (names, _, bounds) = self.parse_type_param_names_bounds_and_const_flags()
  (names, bounds)
}

///|
/// Like `parse_type_param_names_and_bounds` but also returns a
/// parallel array of `const`-modifier flags (one per parameter).
/// `<const T extends ...>` flips the matching `const_flags` entry to
/// `true`; subsequent generic-inference passes consult this to skip
/// literal widening on `T`.
fn Parser::parse_type_param_names_bounds_and_const_flags(
  self : Parser,
) -> (Array[String], Array[Bool], Array[(String, @ast.TsType)]) {
  let names : Array[String] = []
  let const_flags : Array[Bool] = []
  let bounds : Array[(String, @ast.TsType)] = []
  self.last_type_params_had_default = false
  self.last_type_params_min_required = 0
  // Tracked in a local (not the parser field) because parsing a parameter's
  // bound can recursively invoke this function for a nested generic function
  // type and clobber the field; the field is assigned once at the end from
  // this local so the outermost call's value wins.
  let mut had_default = false
  // Index of the first defaulted parameter (-1 = none yet). TS requires
  // defaulted parameters to be trailing, so this is also the minimum legal
  // type-argument count for the declaration.
  let mut min_required = -1
  if !self.check(Lt) {
    return (names, const_flags, bounds)
  }
  let _ = self.advance()
  while !self.check(Eof) {
    if self.check(Gt) {
      let _ = self.advance()
      break
    }
    // Optional `const` modifier introduced in TypeScript 5.0:
    // `<const T extends ...>`. Captured per-parameter so the
    // generic-inference pass can keep literal types narrow.
    let is_const = if self.check(Const) {
      let _ = self.advance()
      true
    } else {
      false
    }
    // Variance modifiers `in` / `out` (TypeScript 4.7+):
    // `<out T extends ..., in U, in out V>`. Both keywords are
    // context-sensitive and only act as modifiers when followed by an
    // identifier; they don't affect the erased generic shape, so consume
    // and discard. `in` lexes as the `In` keyword, `out` lexes as a plain
    // identifier.
    let mut consumed_variance = true
    while consumed_variance {
      consumed_variance = false
      if self.check(In) {
        match self.peek_at(1).kind {
          Ident(_) => {
            let _ = self.advance()
            consumed_variance = true
          }
          _ => ()
        }
      }
      match self.peek().kind {
        Ident("out") =>
          match self.peek_at(1).kind {
            Ident(_) => {
              let _ = self.advance()
              consumed_variance = true
            }
            _ => ()
          }
        _ => ()
      }
    }
    let mut name = ""
    if self.is_qualified_type_name_part(0) {
      name = self.parse_binding_ident() catch { _ => "_" }
    } else {
      self.skip_type_param_tail_until_delim()
      if self.check(Comma) {
        let _ = self.advance()
        continue
      }
      if self.check(Gt) {
        let _ = self.advance()
      }
      break
    }
    names.push(name)
    const_flags.push(is_const)
    // Record every type-parameter name seen anywhere (class / interface /
    // alias / function / method / call+construct-signature generics). The
    // TS2314 bare-reference check excludes these names: a bare `Named(X)` that
    // is also a type-parameter name might be an in-scope type parameter rather
    // than a reference to a same-named generic declaration (e.g. `new <Y, Z,
    // A, B>(...)` uses `B` as a type parameter while a class `B<U, V>` also
    // exists), so skipping it keeps the check false-positive-free.
    self.interface_member_modifier_misuses.push("<type-param-name>\{name}")
    if self.match_(Extends) {
      let saved_pos = self.pos
      let saved_pending_gt = self.pending_gt
      let saved_pending_gt_pos = self.pending_gt_pos
      try {
        let bound = self.parse_type()
        bounds.push((name, bound))
      } catch {
        _ => {
          self.pos = saved_pos
          self.pending_gt = saved_pending_gt
          self.pending_gt_pos = saved_pending_gt_pos
          self.skip_type_param_tail_until_delim()
        }
      }
    }
    if self.match_(Eq) {
      had_default = true
      if min_required < 0 {
        min_required = names.length() - 1
      }
      self.skip_type_param_tail_until_delim()
    }
    if self.check(Comma) {
      let _ = self.advance()
      continue
    }
    if self.check(Gt) {
      let _ = self.advance()
      break
    }
  }
  self.last_type_params_had_default = had_default
  self.last_type_params_min_required = if min_required < 0 {
    names.length()
  } else {
    min_required
  }
  (names, const_flags, bounds)
}

///|
fn Parser::push_local_type_param_bounds(
  self : Parser,
  bounds : Array[(String, @ast.TsType)],
) -> Int {
  let old_len = self.local_type_param_bounds.length()
  for bound in bounds {
    self.local_type_param_bounds.push(bound)
  }
  old_len
}

///|
fn Parser::restore_local_type_param_bounds(
  self : Parser,
  old_len : Int,
) -> Unit {
  while self.local_type_param_bounds.length() > old_len {
    let _ = self.local_type_param_bounds.pop()
  }
}

///|
/// Parse the `extends Foo<A>, Bar.Baz, Qux<X, Y>` clause and return both
/// the base names and the per-base type-argument lists. Used by the
/// interface parser so the checker can substitute `<A>` when walking
/// the inheritance chain.
fn Parser::parse_extends_clause_until_body(
  self : Parser,
) -> (Array[String], Array[Array[@ast.TsType]]) {
  let names : Array[String] = []
  let args_per_name : Array[Array[@ast.TsType]] = []
  if !self.check(Extends) {
    return (names, args_per_name)
  }
  let _ = self.advance() // consume extends
  // TS1097: `interface I extends { }` — the list cannot be empty. Same
  // "the body opens immediately" test as the `implements` clause; see
  // `parse_implements_names`. A `{` here is an error under every spelling
  // (`extends { }` is TS1097, `extends {a: number}` is TS1005), so the
  // test cannot fire on legal input.
  if self.check(LBrace) {
    self.record_suppressible_grammar_misuse("an `extends` list cannot be empty")
  }
  let mut paren_depth = 0
  let mut bracket_depth = 0
  let mut angle_depth = 0
  let mut expect_name = true
  while !self.check(Eof) {
    match self.peek().kind {
      LBrace if paren_depth == 0 && bracket_depth == 0 && angle_depth == 0 =>
        break
      // TS2499 ("An interface can only extend an identifier /
      // qualified-name with optional type arguments"):
      // `interface C1 extends Foo?.Bar {}` is not a qualified name. This
      // is batch CN's TS2500 for `implements` at the OTHER clause that
      // takes a qualified name, and the asymmetry that made TS2500
      // belong to `implements` alone does not apply here: a CLASS
      // `extends` takes an expression (so `class C extends A?.B {}` is
      // legal), but an INTERFACE `extends` takes a type reference. This
      // function has exactly one caller, `parse_interface`, so the
      // distinction is structural rather than a condition.
      Question if paren_depth == 0 &&
        bracket_depth == 0 &&
        angle_depth == 0 &&
        self.peek_at(1).kind is Dot => {
        self.grammar_misuses.push(
          "an interface can only extend an identifier / qualified-name with optional type arguments",
        )
        self.advance() |> ignore
      }
      Ident(name) if expect_name &&
        paren_depth == 0 &&
        bracket_depth == 0 &&
        angle_depth == 0 => {
        let mut full_name = name
        let _ = self.advance()
        while self.check(Dot) {
          match self.peek_at(1).kind {
            Ident(part) => {
              let _ = self.advance()
              let _ = self.advance()
              full_name = full_name + "." + part
            }
            _ => break
          }
        }
        // Capture type arguments if the next token is `<`. Defensive:
        // fall back to the depth-counting skip when parse_type throws
        // (e.g. on malformed input).
        let base_args : Array[@ast.TsType] = []
        if self.check(Lt) && paren_depth == 0 && bracket_depth == 0 {
          let saved = self.pos
          try {
            let _ = self.expect(Lt)
            let first = self.parse_type()
            base_args.push(first)
            while self.match_(Comma) {
              if self.check(Gt) || self.check(GtGt) || self.check(GtGtGt) {
                break
              }
              base_args.push(self.parse_type())
            }
            // Accept `>` / `>>` / `>>>` as the closing token; the parser
            // already handles the `>>`/`>>>` ASR-rewrite for nested
            // type-arg lists.
            if self.match_(Gt) {
              ()
            } else if self.check(GtGt) {
              // Consume one `>` and leave a synthetic `Gt` for the outer
              // context — easiest: advance and let the outer depth
              // counter resync.
              let _ = self.advance()
            } else if self.check(GtGtGt) {
              let _ = self.advance()
            }
          } catch {
            _ => {
              self.pos = saved
              base_args.clear()
            }
          } noraise {
            _ => ()
          }
        }
        names.push(full_name)
        args_per_name.push(base_args)
        expect_name = false
      }
      LParen => {
        paren_depth += 1
        let _ = self.advance()
      }
      RParen => {
        if paren_depth > 0 {
          paren_depth -= 1
        }
        let _ = self.advance()
      }
      LBracket => {
        bracket_depth += 1
        let _ = self.advance()
      }
      RBracket => {
        if bracket_depth > 0 {
          bracket_depth -= 1
        }
        let _ = self.advance()
      }
      Lt => {
        angle_depth += 1
        let _ = self.advance()
      }
      Gt => {
        if angle_depth > 0 {
          angle_depth -= 1
        }
        let _ = self.advance()
      }
      GtGt => {
        angle_depth = if angle_depth >= 2 { angle_depth - 2 } else { 0 }
        let _ = self.advance()
      }
      GtGtGt => {
        angle_depth = if angle_depth >= 3 { angle_depth - 3 } else { 0 }
        let _ = self.advance()
      }
      Comma => {
        if paren_depth == 0 && bracket_depth == 0 && angle_depth == 0 {
          expect_name = true
        }
        let _ = self.advance()
      }
      _ => {
        let _ = self.advance()
      }
    }
  }
  (names, args_per_name)
}

///|
// True when an accessibility modifier (`private` / `public` / `protected`) is
// being used as a *modifier* on the next interface / type-literal member — i.e.
// it's one of those keywords AND the following token begins a member name. Such
// a modifier is illegal on a type member (TS1070); the caller records the
// diagnostic, consumes the keyword, and parses the member as usual. Reuses the
// same member-start lookahead as the `readonly` modifier so a property literally
// named `private` (`{ private: string }`) is not misread as a modifier.
fn Parser::can_consume_interface_accessibility_modifier(self : Parser) -> Bool {
  let is_acc = match self.peek().kind {
    Ident(n) => n == "private" || n == "public" || n == "protected"
    _ => false
  }
  if !is_acc {
    return false
  }
  // A modifier must sit on the SAME line as the member it modifies. With a line
  // break after the keyword, ASI splits it into its own property signature
  // (`interface I { protected \n p }` declares members `protected` and `p`,
  // which TS accepts) — so a newline before the next token means this is a
  // member *name*, not a modifier.
  let next = self.peek_at(1)
  if next.has_newline_before {
    return false
  }
  token_starts_interface_member(next.kind)
}

///|
fn Parser::can_consume_interface_readonly_modifier(self : Parser) -> Bool {
  if !self.check(Ident("readonly")) {
    return false
  }
  token_starts_interface_member(self.peek_at(1).kind)
}

///|
// True when `kind` can begin an interface / type-literal member name (a plain
// identifier, string / numeric literal key, index-signature `[`, call / new
// signature, or any of the contextual keywords that double as member names).
fn token_starts_interface_member(kind : TokenKind) -> Bool {
  match kind {
    Ident(_)
    | Str(_)
    | Int(_)
    | Number(_)
    | LBracket
    | New
    | Finally
    | Try
    | Catch
    | Throw
    | Delete
    | Debugger
    | Type
    | In
    | Of
    | For
    | From
    | With
    | Typeof
    | Return
    | If
    | Else
    | While
    | Do
    | Switch
    | Case
    | Default
    | Break
    | Continue
    | Extends
    | Class
    | Function
    | Var
    | Let
    | Const
    | StringType
    | NumberType
    | BooleanType
    | VoidType
    | IntType
    | Import
    | Export
    | As
    | Yield
    | Instanceof
    | Declare => true
    _ => false
  }
}

///|
// / interface
pub fn Parser::parse_interface(
  self : Parser,
) -> @ast.TsInterface raise ParseError {
  let _ = self.expect(Interface)
  let name = match self.advance().kind {
    Ident(n) => n
    k => raise ParseError("Expected interface name, got \{k}")
  }
  let (type_params, const_flags, type_param_bounds) = self.parse_type_param_names_bounds_and_const_flags()
  // TS2314: a generic interface with no defaulted type parameter requires all
  // of its type arguments, so a bare reference (`I` without `<...>`) is an
  // error. Recorded via the shared channel; the checker consults it.
  if type_params.length() > 0 && not(self.last_type_params_had_default) {
    self.interface_member_modifier_misuses.push(
      "<generic-noarg-required>\{name}",
    )
  }
  // Minimum legal type-argument count (leading parameters without a
  // default). Consulted by the checker's TS2314 under-application check so
  // `Iterable<T, TReturn = any, TNext = any>` accepts 1..3 arguments.
  if type_params.length() > 0 {
    self.interface_member_modifier_misuses.push(
      "<generic-min-arity>\{name}=\{self.last_type_params_min_required}",
    )
  }
  // TS1277: a `const` modifier on a type parameter is only valid on a
  // function, method, or class. An interface's own type-parameter list is
  // none of those, so `interface I<const T>` is always an error. Record it
  // through the shared interface-misuse channel with a distinct sentinel.
  for i, name_i in type_params {
    if i < const_flags.length() && const_flags[i] {
      self.interface_member_modifier_misuses.push("<const-typeparam>\{name_i}")
    }
  }
  let local_type_param_bound_len = self.push_local_type_param_bounds(
    type_param_bounds,
  )
  let (extends_names, extends_args) = self.parse_extends_clause_until_body()
  let _ = self.expect(LBrace)
  let fields : Array[(String, @ast.TsType)] = []
  let readonly_fields : Array[String] = []
  let index_signatures : Array[(@ast.TsType, @ast.TsType)] = []
  let method_type_params : Array[(String, Array[String])] = []
  let method_type_param_bounds : Array[(String, Array[(String, @ast.TsType)])] = []
  let internal_members : Array[String] = []
  let deprecated_members : Array[String] = []
  // TS2386 support: the `?` of the first METHOD signature seen for each
  // name in THIS body. A later signature of the same name that disagrees
  // is the error — `m?(x: number): number; m(s: string): string`. Only
  // methods participate: two same-named PROPERTIES are a duplicate
  // identifier (TS2300 / TS2717), a different error that already fires.
  let method_sig_optional : Map[String, (Bool, Bool)] = {}
  while !self.check(RBrace) && !self.check(Eof) {
    // A JSDoc block directly above the member attaches to its first
    // token (which may be a `readonly` / accessibility modifier, so the
    // position is captured before modifier consumption). `@internal` /
    // `@deprecated` tags are recorded per member name below.
    let mut member_is_internal = false
    let mut member_is_deprecated = false
    match self.jsdoc_at_pos.get(self.peek().pos) {
      Some(jb) =>
        for t in jb.tags {
          match t {
            Internal => member_is_internal = true
            Deprecated(_) => member_is_deprecated = true
            _ => ()
          }
        }
      None => ()
    }
    // TS1070: an accessibility modifier (`private` / `public` / `protected`)
    // is never valid on an interface member. Record it, consume the keyword,
    // and parse the member as usual (the `readonly` modifier — which IS legal —
    // is handled separately below). `name` is the member it precedes.
    while self.can_consume_interface_accessibility_modifier() {
      let _ = self.advance()
      let member = match self.peek().kind {
        Ident(n) => n
        _ => "<member>"
      }
      self.interface_member_modifier_misuses.push("\{name}.\{member}")
    }
    // Skip readonly modifier
    let mut is_readonly_field = false
    if self.can_consume_interface_readonly_modifier() {
      let _ = self.advance()
      is_readonly_field = true
    }
    // Interface accessor members (TS 5.4+, used by modern lib files:
    // `get maxByteLength(): number;` in lib.es2024.arraybuffer). Consume
    // the `get` / `set` keyword and treat the member as a plain property
    // — a getter contributes its return type (readonly), a setter its
    // parameter type. Bare members NAMED `get` / `set` (followed by
    // `(` / `<` / `:` / `?`) keep the old path.
    let mut accessor_kind : String? = None
    match self.peek().kind {
      Ident(n) if (n == "get" || n == "set") &&
        self.peek_at(1).kind != LParen &&
        self.peek_at(1).kind != Lt &&
        self.peek_at(1).kind != Colon &&
        self.peek_at(1).kind != Question &&
        self.peek_at(1).kind != Semicolon &&
        self.peek_at(1).kind != Comma &&
        self.peek_at(1).kind != RBrace => {
        let _ = self.advance()
        accessor_kind = Some(n)
      }
      _ => ()
    }
    // Parse field/method name
    let field_name : String = match self.peek().kind {
      // Call signature: (...): T
      LParen => "<call>"
      // Generic call signature: <T>(...): U
      Lt => {
        self.skip_type_params()
        "<call>"
      }
      // Constructor signature: new(...): T
      New => {
        let _ = self.advance()
        "<new>"
      }
      // Computed property / index signature: [key: T]: U or [Symbol.xxx]: T
      LBracket => {
        let _ = self.advance()
        // An index signature has the canonical form `[<ident>: <type>]: <type>`,
        // distinguished from a computed property like `[Symbol.iterator]` by
        // the `<ident>` immediately followed by `:`. When recognised we parse
        // the key type directly so later passes can lower the signature into
        // `op_get` / `op_set` accessors instead of dropping the field.
        let is_index_sig_start = match self.peek().kind {
          Ident(_) => self.peek_at(1).kind == Colon
          _ => false
        }
        if is_index_sig_start {
          let _ = self.advance() // consume index name (ignored)
          let _ = self.expect(Colon)
          let key_type = self.parse_type()
          self.record_index_signature_key_misuse(key_type)
          let _ = self.expect(RBracket)
          // Optional `?` is not meaningful on index signatures; consume to be
          // forgiving but ignore.
          let _ = self.match_(Question)
          let value_type = if self.check(Colon) {
            let _ = self.advance()
            self.parse_type()
          } else {
            self.record_index_signature_missing_annotation()
            @ast.TsType::Any
          }
          // Stash on a dedicated index_signatures channel so bridge passes
          // can lower the dynamic key/value pair to `op_get`/`op_set`
          // accessors without contaminating the regular named-field walk.
          index_signatures.push((key_type, value_type))
          let _ = is_readonly_field
          if self.check(Semicolon) || self.check(Comma) {
            let _ = self.advance()
          }
          continue
        }
        self.record_index_signature_shape_misuse()
        self.record_computed_member_key_misuse("an interface")
        // TS2304: a computed member key that is a single bare identifier
        // (`[e]: number` / `[e](): T`) references the value namespace --
        // record it for the checker's undeclared-name resolution. Dotted
        // keys (`[Symbol.iterator]`) don't match the single-token shape.
        match self.peek().kind {
          Ident(key) if self.peek_at(1).kind == RBracket && !self.in_function =>
            self.grammar_misuses.push("<computed-type-key>" + key)
          _ => ()
        }
        // A WELL-KNOWN-symbol key (`[Symbol.iterator]: T`) keeps a stable
        // `@@`-name so the member participates in assignability
        // (symbolProperty9/10/12); other computed keys stay opaque.
        let mut symbol_name : String? = None
        match (self.peek().kind, self.peek_at(2).kind, self.peek_at(3).kind) {
          (Ident("Symbol"), Ident(sym), RBracket) if self.peek_at(1).kind == Dot => {
            // TS2464: `Symbol.for` / `Symbol.keyFor` are FUNCTIONS on
            // SymbolConstructor, not symbols — they can never be a
            // computed property key (symbolProperty59).
            if sym == "keyFor" || sym == "for" {
              self.grammar_misuses.push(
                "a computed property name must be of type `string`, `number`, `symbol`, or `any` — `Symbol.\{sym}` is a function",
              )
            }
            let _ = self.advance()
            let _ = self.advance()
            let _ = self.advance()
            let _ = self.expect(RBracket)
            symbol_name = Some("@@" + sym)
          }
          _ => ()
        }
        match symbol_name {
          Some(sn) => sn
          None => {
            let mut saw_colon = false
            while !self.check(RBracket) && !self.check(Eof) {
              if self.check(Colon) {
                saw_colon = true
              }
              let _ = self.advance()
            }
            let _ = self.expect(RBracket)
            if saw_colon {
              "<index_signature>"
            } else {
              "<computed>"
            }
          }
        }
      }
      // Regular identifier
      Ident(n) => {
        let _ = self.advance()
        n
      }
      // String literal as field name: "fieldName"
      Str(s) => {
        let _ = self.advance()
        s
      }
      // Number literal as field name: 0, 1, etc. Both `Int` (integers that
      // fit in i32) and `Number` (anything else: `1.0`, `1e9`, etc.) are
      // valid TS property keys.
      Int(n) => {
        let _ = self.advance()
        n.to_string()
      }
      Number(d) => {
        let _ = self.advance()
        d.to_string()
      }
      // Keywords as field names (TypeScript allows this)
      Finally => {
        let _ = self.advance()
        "finally"
      }
      Try => {
        let _ = self.advance()
        "try"
      }
      Catch => {
        let _ = self.advance()
        "catch"
      }
      Throw => {
        let _ = self.advance()
        "throw"
      }
      Delete => {
        let _ = self.advance()
        "delete"
      }
      Debugger => {
        let _ = self.advance()
        "debugger"
      }
      Type => {
        let _ = self.advance()
        "type"
      }
      In => {
        let _ = self.advance()
        "in"
      }
      Of => {
        let _ = self.advance()
        "of"
      }
      For => {
        let _ = self.advance()
        "for"
      }
      From => {
        let _ = self.advance()
        "from"
      }
      With => {
        let _ = self.advance()
        "with"
      }
      Typeof => {
        let _ = self.advance()
        "typeof"
      }
      Return => {
        let _ = self.advance()
        "return"
      }
      If => {
        let _ = self.advance()
        "if"
      }
      Else => {
        let _ = self.advance()
        "else"
      }
      While => {
        let _ = self.advance()
        "while"
      }
      Do => {
        let _ = self.advance()
        "do"
      }
      Switch => {
        let _ = self.advance()
        "switch"
      }
      Case => {
        let _ = self.advance()
        "case"
      }
      Default => {
        let _ = self.advance()
        "default"
      }
      Break => {
        let _ = self.advance()
        "break"
      }
      Continue => {
        let _ = self.advance()
        "continue"
      }
      Extends => {
        let _ = self.advance()
        "extends"
      }
      Class => {
        let _ = self.advance()
        "class"
      }
      Function => {
        let _ = self.advance()
        "function"
      }
      Var => {
        let _ = self.advance()
        "var"
      }
      Let => {
        let _ = self.advance()
        "let"
      }
      Const => {
        let _ = self.advance()
        "const"
      }
      StringType => {
        let _ = self.advance()
        "string"
      }
      NumberType => {
        let _ = self.advance()
        "number"
      }
      BooleanType => {
        let _ = self.advance()
        "boolean"
      }
      VoidType => {
        let _ = self.advance()
        "void"
      }
      IntType => {
        let _ = self.advance()
        "int"
      }
      Import => {
        let _ = self.advance()
        "import"
      }
      Export => {
        let _ = self.advance()
        "export"
      }
      As => {
        let _ = self.advance()
        "as"
      }
      Yield => {
        let _ = self.advance()
        "yield"
      }
      Instanceof => {
        let _ = self.advance()
        "instanceof"
      }
      Declare => {
        let _ = self.advance()
        "declare"
      }
      // Literal-keyword identifiers also stand in as property names.
      Bool(true) => {
        let _ = self.advance()
        "true"
      }
      Bool(false) => {
        let _ = self.advance()
        "false"
      }
      Null => {
        let _ = self.advance()
        "null"
      }
      // Additional reserved words allowed as interface field names —
      // TS accepts every keyword in property-name position.
      Interface => {
        let _ = self.advance()
        "interface"
      }
      k =>
        raise ParseError("Expected field name at \{self.peek().pos}, got \{k}")
    }
    // Capture method-level type parameters `<T>` so the checker can keep
    // a generic method's shadowing type parameter distinct from the
    // interface's own. (Only meaningful for method signatures; harmless
    // on properties, which never carry `<...>`.) Constraint bounds are
    // kept alongside so the bridge can substitute them at the FFI
    // boundary (`pipe<T extends $ZodType>` types its param as `$ZodType`).
    let (m_type_params, m_type_param_bounds) = self.parse_type_param_names_and_bounds()
    // Check for optional marker ?
    let is_optional = self.match_(Question)
    // Method signature: name(...): T or name<T>(...): U
    if self.check(LParen) {
      let _ = self.advance()
      // TS2386: "Overload signatures must all be optional or required."
      // Probed — an object TYPE, an interface and a class all report when
      // two same-named method signatures disagree on `?`, and both-optional
      // / both-required / different-names are all accepted. Purely about
      // the `?`, so no type is consulted.
      self.record_overload_optionality(
        method_sig_optional, field_name, field_name, is_optional,
      )
      let method_params = self.parse_params()
      let _ = self.expect(RParen)
      // TS2371: a parameter initializer (`x = 1`) is only allowed in an
      // implementation signature. An interface method / call signature never
      // has a body, so any default here is an error. Recorded via the shared
      // interface-member-misuse channel with a `<param-init>` sentinel.
      for param in method_params {
        if !(param.default is None) {
          self.interface_member_modifier_misuses.push(
            "<param-init>\{field_name}.\{param.name}",
          )
        }
      }
      // Return type
      let had_return_annotation = self.check(Colon)
      let return_type = if had_return_annotation {
        let _ = self.advance()
        self.parse_type()
      } else {
        @ast.TsType::Void
      }
      // TS7010 / TS7013 / TS7020: an interface member signature with no
      // return annotation. Accessors get TS7033, a different rule.
      if accessor_kind is None {
        self.record_implicit_any_member_return(
          field_name, had_return_annotation,
        )
      }
      let param_types : Array[@ast.TsType] = []
      for param in method_params {
        param_types.push(param.type_)
      }
      // Accessor members contribute PROPERTY types, not method types: a
      // getter its return type (readonly), a setter its parameter type.
      // A get/set pair for the same name dedups to one field entry.
      let field_type = match accessor_kind {
        Some("get") => {
          readonly_fields.push(field_name)
          return_type
        }
        Some("set") =>
          if param_types.length() > 0 {
            param_types[0]
          } else {
            @ast.TsType::Any
          }
        _ => callable_func_type_from_params(method_params, return_type)
      }
      let accessor_pair_seen = accessor_kind is Some(_) &&
        fields.iter().any(fn(f) { f.0 == field_name })
      if field_name != "<index_signature>" && !accessor_pair_seen {
        fields.push(
          (
            field_name,
            if is_optional {
              wrap_optional_ts_type(field_type)
            } else {
              field_type
            },
          ),
        )
        if m_type_params.length() > 0 {
          method_type_params.push((field_name, m_type_params))
          if m_type_param_bounds.length() > 0 {
            method_type_param_bounds.push((field_name, m_type_param_bounds))
          }
        }
      }
    } else if self.check(Colon) {
      // Property: name: T
      let _ = self.advance()
      let field_type = self.parse_type()
      if field_name != "<index_signature>" {
        fields.push(
          (
            field_name,
            if is_optional {
              wrap_optional_ts_type(field_type)
            } else {
              field_type
            },
          ),
        )
      }
    } else {
      // No type annotation - use Any
      let field_type = @ast.TsType::Any
      if field_name != "<index_signature>" {
        fields.push(
          (
            field_name,
            if is_optional {
              wrap_optional_ts_type(field_type)
            } else {
              field_type
            },
          ),
        )
      }
    }
    if is_readonly_field {
      readonly_fields.push(field_name)
    }
    if field_name != "<index_signature>" &&
      field_name != "<call>" &&
      field_name != "<new>" &&
      field_name != "<computed>" {
      if member_is_internal && !internal_members.contains(field_name) {
        internal_members.push(field_name)
      }
      if member_is_deprecated && !deprecated_members.contains(field_name) {
        deprecated_members.push(field_name)
      }
    }
    // Skip trailing semicolon or comma (both optional)
    if self.check(Semicolon) || self.check(Comma) {
      let _ = self.advance()
    }
  }
  let _ = self.expect(RBrace)
  self.restore_local_type_param_bounds(local_type_param_bound_len)
  {
    name,
    type_params,
    type_param_bounds,
    extends_names,
    extends_args,
    fields,
    readonly_fields,
    index_signatures,
    method_type_params,
    method_type_param_bounds,
    internal_members,
    deprecated_members,
  }
}

///|
/// Skip until semicolon or end of file, respecting brace/bracket/paren nesting
fn Parser::skip_until_semicolon(self : Parser) -> Unit {
  let mut brace_depth = 0
  let mut bracket_depth = 0
  let mut paren_depth = 0
  while !self.check(Eof) {
    match self.peek().kind {
      Semicolon => {
        if brace_depth == 0 && bracket_depth == 0 && paren_depth == 0 {
          let _ = self.advance()
          return
        }
        let _ = self.advance()
      }
      LBrace => {
        brace_depth += 1
        let _ = self.advance()
      }
      RBrace => {
        brace_depth -= 1
        let _ = self.advance()
      }
      LBracket => {
        bracket_depth += 1
        let _ = self.advance()
      }
      RBracket => {
        bracket_depth -= 1
        let _ = self.advance()
      }
      LParen => {
        paren_depth += 1
        let _ = self.advance()
      }
      RParen => {
        paren_depth -= 1
        let _ = self.advance()
      }
      _ => {
        let _ = self.advance()
      }
    }
  }
}

///|
fn type_alias_object_fields_to_interface(
  name : String,
  type_params : Array[String],
  type_ : @ast.TsType,
) -> @ast.TsInterface? {
  match type_ {
    @ast.TsType::Object(fields) => {
      let iface_fields : Array[(String, @ast.TsType)] = []
      for field in fields {
        let (key_type, value_type) = field
        match key_type {
          @ast.TsType::Literal(field_name) =>
            iface_fields.push((field_name, value_type))
          _ => return None
        }
      }
      Some({
        name,
        type_params,
        extends_names: [],
        extends_args: [],
        fields: iface_fields,
        readonly_fields: [],
        index_signatures: [],
        type_param_bounds: [],
        method_type_params: [],
        method_type_param_bounds: [],
        internal_members: [],
        deprecated_members: [],
      })
    }
    _ => None
  }
}

///|
fn type_alias_to_interface(type_alias : @ast.TsTypeAlias) -> @ast.TsInterface? {
  // The conversion from `type X<T extends Bound> = { ... }` to interface is
  // already lossy on T (the resulting interface is treated structurally,
  // without the generic flowing through bridge synthesis). When the alias has
  // bounds, substitute every reference to a bounded type-param with the bound
  // BEFORE conversion. This recovers concrete shapes at value positions like
  // `form: Record<string, T | T[]>` → `Record<string, FormValue | FormValue[]>`
  // while leaving non-Object aliases (e.g. `DateArg<DateType> = DateType | ...`)
  // untouched, since `type_alias_object_fields_to_interface` only fires on
  // Object bodies.
  let body = if type_alias.type_param_bounds.length() > 0 {
    substitute_alias_param_bounds(
      type_alias.type_,
      type_alias.type_param_bounds,
    )
  } else {
    type_alias.type_
  }
  type_alias_object_fields_to_interface(
    type_alias.name,
    type_alias.type_params,
    body,
  )
}

///|
// Walk `type_` and substitute every `Named(name)` reference whose `name`
// appears in `bounds` with that name's bound. Operates structurally over
// every TsType variant. Used by `type_alias_to_interface` to project a
// generic Object-alias body into a concrete interface body.
fn substitute_alias_param_bounds(
  type_ : @ast.TsType,
  bounds : Array[(String, @ast.TsType)],
) -> @ast.TsType {
  fn lookup_bound(name : String) -> @ast.TsType? {
    for entry in bounds {
      if entry.0 == name {
        return Some(entry.1)
      }
    }
    None
  }

  fn walk(t : @ast.TsType) -> @ast.TsType {
    match t {
      Named(name) =>
        match lookup_bound(name) {
          Some(b) => b
          None => t
        }
      Applied(name, args) => {
        let new_args : Array[@ast.TsType] = []
        for a in args {
          new_args.push(walk(a))
        }
        Applied(name, new_args)
      }
      Array(inner) => Array(walk(inner))
      Tuple(items) => {
        let out : Array[@ast.TsType] = []
        for it in items {
          out.push(walk(it))
        }
        Tuple(out)
      }
      Object(fields) => {
        let out : Array[(@ast.TsType, @ast.TsType)] = []
        for f in fields {
          out.push((walk(f.0), walk(f.1)))
        }
        Object(out)
      }
      Func(params, ret) => {
        let new_params : Array[@ast.TsType] = []
        for p in params {
          new_params.push(walk(p))
        }
        Func(new_params, walk(ret))
      }
      Union(items) => {
        let out : Array[@ast.TsType] = []
        for it in items {
          out.push(walk(it))
        }
        Union(out)
      }
      Intersection(items) => {
        let out : Array[@ast.TsType] = []
        for it in items {
          out.push(walk(it))
        }
        Intersection(out)
      }
      Conditional(c, e, tb, fb) =>
        Conditional(walk(c), walk(e), walk(tb), walk(fb))
      IndexedAccess(base, k) => IndexedAccess(walk(base), walk(k))
      _ => t
    }
  }

  walk(type_)
}

///|
fn Parser::parse_type_alias_after_type_keyword(
  self : Parser,
) -> @ast.TsTypeAlias raise ParseError {
  let name = self.parse_binding_ident()
  // Capture the bounds in the AST so bridge passes can consult them at
  // use sites; do NOT push them as local bindings, because doing so would let
  // `parse_type` substitute `Named(T) → bound` inside the alias body, eagerly
  // erasing the generic shape (e.g. `DateArg<DateType>` would become
  // `Date | ...` and lose precision).
  let (type_params, const_flags, type_param_bounds) = self.parse_type_param_names_bounds_and_const_flags()
  // TS2314: a generic type alias with no defaulted type parameter requires all
  // of its type arguments; a bare reference (`A` without `<...>`) is an error.
  // Read before the alias body is parsed (which could clobber the flag via a
  // nested generic).
  if type_params.length() > 0 && not(self.last_type_params_had_default) {
    self.interface_member_modifier_misuses.push(
      "<generic-noarg-required>\{name}",
    )
  }
  if type_params.length() > 0 {
    self.interface_member_modifier_misuses.push(
      "<generic-min-arity>\{name}=\{self.last_type_params_min_required}",
    )
  }
  // TS1277: a `const` modifier on a type-alias type parameter is invalid
  // (only functions, methods, and classes allow it). `type T<const U> = …`.
  for i, name_i in type_params {
    if i < const_flags.length() && const_flags[i] {
      self.interface_member_modifier_misuses.push("<const-typeparam>\{name_i}")
    }
  }
  let _ = self.expect(Eq)
  let alias_type = self.parse_type()
  self.consume_semicolon()
  { name, type_params, type_param_bounds, type_: alias_type }
}

///|
fn Parser::parse_type_alias(self : Parser) -> @ast.TsTypeAlias raise ParseError {
  let _ = self.expect(Type)
  self.parse_type_alias_after_type_keyword()
}

///|
/// Skip declaration block (namespace, module, class, enum, etc.)
fn Parser::skip_declaration_block(self : Parser) -> Unit {
  // Skip until we find { or ;
  while !self.check(LBrace) && !self.check(Semicolon) && !self.check(Eof) {
    let _ = self.advance()
  }
  if self.check(Semicolon) {
    let _ = self.advance()
    return
  }
  if self.check(LBrace) {
    let _ = self.advance()
    let mut depth = 1
    while depth > 0 && !self.check(Eof) {
      match self.peek().kind {
        LBrace => {
          depth += 1
          let _ = self.advance()
        }
        RBrace => {
          depth -= 1
          let _ = self.advance()
        }
        _ => {
          let _ = self.advance()
        }
      }
    }
  }
}

///|
// / declare function (without 'declare function' prefix - already consumed)

///|
/// name(params): returnType;
pub fn Parser::parse_declare_function(
  self : Parser,
) -> @ast.TsImport raise ParseError {

  // function
  let name = self.parse_binding_ident()

  let (type_params, type_param_bounds) = self.parse_type_param_names_and_bounds()
  let local_type_param_bound_len = self.push_local_type_param_bounds(
    type_param_bounds,
  )
  // parameter
  let _ = self.expect(LParen)
  let params : Array[(String, @ast.TsType)] = []
  let param_is_rest : Array[Bool] = []
  while !self.check(RParen) && !self.check(Eof) {
    // Handle rest parameter: ...name
    let is_rest = self.match_ellipsis()
    let param_name = self.parse_declare_param_name()
    // Handle optional parameter: name?
    let is_optional = self.match_(Question)
    // Type annotation is optional
    let had_param_annotation = self.check(Colon)
    let computed_param_type = if had_param_annotation {
      let _ = self.advance()
      let parsed = self.parse_type()
      if is_optional {
        wrap_optional_ts_type(parsed)
      } else {
        parsed
      }
    } else if is_optional {
      wrap_optional_ts_type(@ast.TsType::Any)
    } else {
      @ast.TsType::Any
    }
    // TS7006 / TS7019 (noImplicitAny): `declare function` parameters have no
    // implementation or contextual source to infer from.
    if self.no_implicit_any && !had_param_annotation && param_name != "this" {
      if is_rest {
        self.grammar_misuses.push(
          "<noimplicitany>rest parameter `\{param_name}` implicitly has an `any[]` type",
        )
      } else {
        self.grammar_misuses.push(
          "<noimplicitany>parameter `\{param_name}` implicitly has an `any` type",
        )
      }
    }
    // TypeScript's `this` parameter at index 0 (`afterAll(this: unknown,
    // fn, timeout)`) types the receiver — there is no runtime arg for
    // it. Drop it so the generated `bridge.js` body doesn't emit
    // `function f(this, ...)`, which Node rejects as a syntax error.
    if !(param_name == "this" && params.length() == 0) {
      params.push((param_name, computed_param_type))
      param_is_rest.push(is_rest)
    }
    if self.check(Comma) {
      let _ = self.advance()
    }
  }
  let _ = self.expect(RParen)

  // return valuetype — annotation is optional (TS treats a missing one as
  // `void` for `declare function`, but we use `Any` so the checker matches
  // the convention used elsewhere for un-annotated functions).
  let had_return_annotation = self.check(Colon)
  let return_type = if had_return_annotation {
    let _ = self.advance()
    self.parse_type()
  } else {
    @ast.TsType::Any
  }
  // TS7010 (noImplicitAny): an ambient function without a return-type
  // annotation implicitly returns `any` -- no body exists to infer from.
  if self.no_implicit_any && !had_return_annotation {
    self.grammar_misuses.push(
      "<noimplicitany>function `\{name}` lacks a return-type annotation and implicitly has an `any` return type",
    )
  }
  self.restore_local_type_param_bounds(local_type_param_bound_len)
  self.consume_semicolon()
  { name, module_: "env", type_params, params, param_is_rest, return_type }
}

///|
fn Parser::parse_declare_param_name(self : Parser) -> String raise ParseError {
  match self.peek().kind {
    Ident(n) => {
      let _ = self.advance()
      n
    }
    StringType => {
      let _ = self.advance()
      "string"
    }
    NumberType => {
      let _ = self.advance()
      "number"
    }
    BooleanType => {
      let _ = self.advance()
      "boolean"
    }
    VoidType => {
      let _ = self.advance()
      "void"
    }
    Type => {
      let _ = self.advance()
      "type"
    }
    New => {
      let _ = self.advance()
      "new"
    }
    Delete => {
      let _ = self.advance()
      "delete"
    }
    Typeof => {
      let _ = self.advance()
      "typeof"
    }
    In => {
      let _ = self.advance()
      "in"
    }
    Of => {
      let _ = self.advance()
      "of"
    }
    From => {
      let _ = self.advance()
      "from"
    }
    As => {
      let _ = self.advance()
      "as"
    }
    LBrace | LBracket => {
      let open_tok = self.peek().kind
      let close_tok : TokenKind = match open_tok {
        LBrace => RBrace
        _ => RBracket
      }
      let _ = self.advance()
      let mut depth = 1
      while depth > 0 && !self.check(Eof) {
        if self.peek().kind == open_tok {
          depth += 1
        } else if self.peek().kind == close_tok {
          depth -= 1
        }
        let _ = self.advance()
      }
      "<destructured>"
    }
    k => raise ParseError("Expected parameter name, got \{k}")
  }
}

///|
fn Parser::parse_declare_values(
  self : Parser,
) -> Array[@ast.TsValueDecl] raise ParseError {
  let values : Array[@ast.TsValueDecl] = []
  // A destructuring pattern in an ambient declaration (`declare var
  // [a, b];`, declarationInAmbientContext) is tolerated: current tsc
  // accepts it (the fixture has no error baseline despite its comment),
  // so just skip the statement without recording anything.
  match self.peek().kind {
    LBracket | LBrace => {
      self.skip_until_semicolon()
      self.consume_semicolon()
      return values
    }
    _ => ()
  }
  // TS7005 (noImplicitAny): an ambient `declare var x;` with neither an
  // annotation nor an initializer implicitly has an `any` type -- unlike a
  // runtime `var x;`, no assignment can ever narrow it.
  fn record_ambient_implicit_any(
    p : Parser,
    vname : String,
    annotated : Bool,
    initialised : Bool,
  ) -> Unit {
    if p.no_implicit_any && !annotated && !initialised {
      p.grammar_misuses.push(
        "<noimplicitany>variable `\{vname}` implicitly has an `any` type",
      )
    }
  }

  let name = self.parse_binding_ident()
  let annotated = self.check(Colon)
  let type_ = self.parse_var_decl_type()
  let initialised = self.match_(Eq)
  if initialised {
    let _ = self.parse_expr()
  }
  record_ambient_implicit_any(self, name, annotated, initialised)
  values.push({ name, type_ })
  while self.match_(Comma) {
    let next_name = self.parse_binding_ident()
    let next_annotated = self.check(Colon)
    let next_type = self.parse_var_decl_type()
    let next_initialised = self.match_(Eq)
    if next_initialised {
      let _ = self.parse_expr()
    }
    record_ambient_implicit_any(
      self, next_name, next_annotated, next_initialised,
    )
    values.push({ name: next_name, type_: next_type })
  }
  self.consume_semicolon()
  values
}

///|
fn Parser::parse_declare_signature_params(
  self : Parser,
) -> Array[(String, @ast.TsType)] raise ParseError {
  let _ = self.expect(LParen)
  let params : Array[(String, @ast.TsType)] = []
  while !self.check(RParen) && !self.check(Eof) {
    let _ = self.match_ellipsis()
    // A parameter-property modifier (`public` / `private` / `protected` /
    // `readonly`) on a declare-class member signature is always TS2369: these
    // signatures have no implementation. A leading modifier is only a modifier
    // when another identifier (the real parameter name, or a further modifier)
    // follows; otherwise the keyword *is* the parameter name (`readonly: T`).
    let mut saw_param_prop_modifier = false
    while true {
      let is_modifier = match self.peek().kind {
        Ident(m) =>
          (
            m == "public" ||
            m == "private" ||
            m == "protected" ||
            m == "readonly"
          ) &&
          self.peek_at(1).kind is Ident(_)
        _ => false
      }
      if is_modifier {
        let _ = self.advance()
        saw_param_prop_modifier = true
      } else {
        break
      }
    }
    let param_name = self.parse_declare_param_name()
    if saw_param_prop_modifier {
      self.param_property_misuses.push(param_name)
    }
    let is_optional = self.match_(Question)
    let base_param_type = if self.check(Colon) {
      let _ = self.advance()
      self.parse_type()
    } else {
      @ast.TsType::Any
    }
    let param_type = if is_optional {
      wrap_optional_ts_type(base_param_type)
    } else {
      base_param_type
    }
    // TypeScript's `this` parameter (`fn(this: T, x: number): void`) types
    // the receiver and is dropped at the JS boundary — there's no runtime
    // arg for it. Skipping it here keeps the generated `bridge.js` from
    // emitting `function f(this, ...)`, which `node` rejects with a
    // `SyntaxError: Unexpected token 'this'`.
    if !(param_name == "this" && params.length() == 0) {
      params.push((param_name, param_type))
    }
    if self.check(Comma) {
      let _ = self.advance()
    }
  }
  let _ = self.expect(RParen)
  params
}

///|

///|
/// Cheap shape check: confirms the token stream starts with `[ Ident :`,
/// the canonical opening of a TS index signature inside a class /
/// interface body. Returns false for the general computed-name pattern
/// `[expr]: T` so the existing skip path still handles it.
fn Parser::is_index_signature_shape_after_lbracket(self : Parser) -> Bool {
  if !self.check(LBracket) {
    return false
  }
  match (self.peek_at(1).kind, self.peek_at(2).kind) {
    (Ident(_), Colon) => true
    _ => false
  }
}

///|
/// Parse a TS index signature inside a class body: `[name: KeyType]:
/// ValueType`. Returns `None` if the shape doesn't match — callers
/// fall back to the original skip path.
fn Parser::try_parse_class_index_signature(
  self : Parser,
) -> (@ast.TsType, @ast.TsType, String?)? raise ParseError {
  let saved = self.pos
  let _ = self.expect(LBracket)
  match self.peek().kind {
    Ident(_) => {
      let _ = self.advance()
    }
    _ => {
      self.pos = saved
      return None
    }
  }
  if !self.match_(Colon) {
    self.pos = saved
    return None
  }
  let key_type = self.parse_type()
  if !self.match_(RBracket) {
    self.pos = saved
    return None
  }
  if !self.match_(Colon) {
    self.pos = saved
    return None
  }
  // A value that is a *naked bounded type parameter* (`[x: number]: T` with
  // `T extends Base` in scope) is about to be widened to its bound by
  // `parse_type`'s constraint inlining. Capture the parameter name first so
  // derived-class index-signature compatibility (TS2415) can still see the
  // generic slot; the widened value stays in the AST for every other
  // consumer.
  let naked_param : String? = match self.peek().kind {
    Ident(n) if self.local_type_param_bound(n) is Some(_) =>
      match self.peek_at(1).kind {
        Semicolon | RBrace => Some(n)
        _ => None
      }
    _ => None
  }
  let value_type = self.parse_type()
  self.consume_semicolon()
  Some((key_type, value_type, naked_param))
}

///|
fn Parser::parse_declare_class_member_name(
  self : Parser,
) -> String? raise ParseError {
  if self.match_(LBracket) {
    // TS2304: a single-bare-identifier computed key (`[e]: number`) in a
    // `declare class` body references the value namespace -- record it for
    // the checker's undeclared-name resolution (dotted keys don't match).
    match self.peek().kind {
      Ident(key) if self.peek_at(1).kind == RBracket && !self.in_function =>
        self.grammar_misuses.push("<computed-type-key>" + key)
      _ => ()
    }
    let mut depth = 1
    while depth > 0 && !self.check(Eof) {
      match self.peek().kind {
        LBracket => {
          depth += 1
          let _ = self.advance()
        }
        RBracket => {
          depth -= 1
          let _ = self.advance()
        }
        _ => {
          let _ = self.advance()
        }
      }
    }
    return None
  }
  let name = match self.advance().kind {
    Ident(n) => n
    PrivateIdent(n) => "#" + n
    Str(s) => s
    Number(n) => "\{n}"
    Int(n) => "\{n}"
    Return => "return"
    If => "if"
    Else => "else"
    For => "for"
    While => "while"
    Break => "break"
    Continue => "continue"
    Function => "function"
    Try => "try"
    Catch => "catch"
    Finally => "finally"
    Throw => "throw"
    Yield => "yield"
    Declare => "declare"
    Type => "type"
    Let => "let"
    Const => "const"
    Var => "var"
    Class => "class"
    New => "new"
    Default => "default"
    Do => "do"
    In => "in"
    Case => "case"
    VoidType => "void"
    With => "with"
    Switch => "switch"
    Typeof => "typeof"
    Delete => "delete"
    Export => "export"
    Import => "import"
    Debugger => "debugger"
    Interface => "interface"
    Instanceof => "instanceof"
    Extends => "extends"
    From => "from"
    As => "as"
    k => raise ParseError("Expected class member name, got \{k}")
  }
  Some(name)
}

///|
fn Parser::parse_declare_class_base_names(
  self : Parser,
) -> (Array[String], Array[String]) {
  let names : Array[String] = []
  let implements_names : Array[String] = []
  let mut paren_depth = 0
  let mut bracket_depth = 0
  let mut brace_depth = 0
  let mut angle_depth = 0
  let mut in_extends = false
  let mut in_implements = false
  let mut expect_name = false
  while !self.check(Eof) {
    match self.peek().kind {
      LBrace if paren_depth == 0 &&
        bracket_depth == 0 &&
        brace_depth == 0 &&
        angle_depth == 0 => break
      Extends if paren_depth == 0 &&
        bracket_depth == 0 &&
        brace_depth == 0 &&
        angle_depth == 0 => {
        in_extends = true
        in_implements = false
        expect_name = true
        let _ = self.advance()
      }
      Ident("implements") if paren_depth == 0 &&
        bracket_depth == 0 &&
        brace_depth == 0 &&
        angle_depth == 0 => {
        in_extends = false
        in_implements = true
        expect_name = true
        let _ = self.advance()
      }
      Ident(name) if (in_extends || in_implements) &&
        expect_name &&
        paren_depth == 0 &&
        bracket_depth == 0 &&
        brace_depth == 0 &&
        angle_depth == 0 => {
        let mut full_name = name
        let _ = self.advance()
        while self.check(Dot) {
          match self.peek_at(1).kind {
            Ident(part) => {
              let _ = self.advance()
              let _ = self.advance()
              full_name = full_name + "." + part
            }
            _ => break
          }
        }
        if in_implements {
          implements_names.push(full_name)
        } else {
          names.push(full_name)
        }
        expect_name = false
      }
      LParen => {
        paren_depth += 1
        let _ = self.advance()
      }
      RParen => {
        if paren_depth > 0 {
          paren_depth -= 1
        }
        let _ = self.advance()
      }
      LBracket => {
        bracket_depth += 1
        let _ = self.advance()
      }
      RBracket => {
        if bracket_depth > 0 {
          bracket_depth -= 1
        }
        let _ = self.advance()
      }
      LBrace => {
        brace_depth += 1
        let _ = self.advance()
      }
      RBrace => {
        if brace_depth > 0 {
          brace_depth -= 1
        }
        let _ = self.advance()
      }
      Lt => {
        angle_depth += 1
        let _ = self.advance()
      }
      Gt => {
        if angle_depth > 0 {
          angle_depth -= 1
        }
        let _ = self.advance()
      }
      GtGt => {
        angle_depth = if angle_depth >= 2 { angle_depth - 2 } else { 0 }
        let _ = self.advance()
      }
      GtGtGt => {
        angle_depth = if angle_depth >= 3 { angle_depth - 3 } else { 0 }
        let _ = self.advance()
      }
      Comma => {
        if (in_extends || in_implements) &&
          paren_depth == 0 &&
          bracket_depth == 0 &&
          brace_depth == 0 &&
          angle_depth == 0 {
          expect_name = true
        }
        let _ = self.advance()
      }
      _ => {
        let _ = self.advance()
      }
    }
  }
  (names, implements_names)
}

///|
fn upsert_class_property(
  properties : Array[@ast.TsClassPropertyDecl],
  property : @ast.TsClassPropertyDecl,
) -> Unit {
  for i in 0..<properties.length() {
    let existing = properties[i]
    if existing.name == property.name &&
      existing.is_static == property.is_static {
      let merged_type = match existing.type_ {
        Any => property.type_
        _ => existing.type_
      }
      properties[i] = {
        name: existing.name,
        type_: merged_type,
        is_static: existing.is_static,
        is_readonly: existing.is_readonly && property.is_readonly,
        is_accessor: false,
        has_initializer: false,
        has_definite_assertion: false,
      }
      return
    }
  }
  properties.push(property)
}

///|
/// Record an ambient (`declare class`) accessor as a METHOD carrying the
/// `get` / `set` tag, mirroring what the runtime class parser does for
/// `class C { get x() {…} }`.
///
/// The property upsert next to each call site is what makes `a.x` read
/// as a property, and it was for a long time the ONLY record — so every
/// consumer that asks "is this base member an accessor or a data
/// property" got the wrong answer for an ambient base. That is one
/// defect with two faces: `declare class A { get x(): string }` beside
/// `class B extends A { x = 1 }` is TS2610 and was silent, while the
/// LEGAL accessor-over-accessor form was reported twice. The corpus has
/// no file of the second shape, which is why FP 0 never caught it.
///
/// A body is `None` (an ambient member has none), which is also what
/// distinguishes these from the runtime path's entries for any consumer
/// that cares.
fn record_ambient_accessor_method(
  methods : Array[@ast.TsClassMethodDecl],
  name : String,
  tag : String,
  is_static : Bool,
  params : Array[(String, @ast.TsType)],
  return_type : @ast.TsType,
) -> Unit {
  methods.push({
    name,
    type_params: [],
    type_param_bounds: [],
    params,
    param_defaults: [],
    param_is_rest: [],
    return_type,
    is_static,
    body: None,
    accessor: tag,
    key_expr: None,
    is_async: false,
    is_generator: false,
  })
}

///|
fn Parser::parse_declare_class(
  self : Parser,
  is_abstract : Bool,
) -> @ast.TsClassDecl raise ParseError {
  let _ = self.expect(Class)
  let name = self.parse_binding_ident()
  let mut class_type_params : Array[String] = []
  let mut class_type_param_bounds : Array[(String, @ast.TsType)] = []
  let mut class_type_param_bound_len = self.local_type_param_bounds.length()
  if self.check(Lt) {
    let (names, type_param_bounds) = self.parse_type_param_names_and_bounds()
    class_type_params = names
    class_type_param_bounds = type_param_bounds
    // TS2314: a generic `declare class` with no defaulted type parameter
    // requires all of its type arguments on a bare reference. Read the flag
    // before the base / body parse clobbers it via a nested generic.
    if name != "" &&
      names.length() > 0 &&
      not(self.last_type_params_had_default) {
      self.interface_member_modifier_misuses.push(
        "<generic-noarg-required>\{name}",
      )
    }
    if name != "" && names.length() > 0 {
      self.interface_member_modifier_misuses.push(
        "<generic-min-arity>\{name}=\{self.last_type_params_min_required}",
      )
    }
    class_type_param_bound_len = self.push_local_type_param_bounds(
      type_param_bounds,
    )
  }
  let (base_names, implements_names) = self.parse_declare_class_base_names()
  let _ = self.expect(LBrace)
  let properties : Array[@ast.TsClassPropertyDecl] = []
  let methods : Array[@ast.TsClassMethodDecl] = []
  let private_members : Array[String] = []
  let protected_members : Array[String] = []
  let abstract_members : Array[String] = []
  let index_signatures : Array[(@ast.TsType, @ast.TsType)] = []
  let static_index_signatures : Array[(@ast.TsType, @ast.TsType)] = []
  let static_blocks : Array[@ast.TsBlock] = []
  let declare_override_names : Array[String] = []
  // TS2386 support: `(first_optional, already_reported)` per member key.
  let declare_method_sig_optional : Map[String, (Bool, Bool)] = {}
  let mut constructor_params : Array[(String, @ast.TsType)]? = None
  let mut saw_public_constructor = false
  let mut saw_nonpublic_constructor = false
  while !self.check(RBrace) && !self.check(Eof) {
    if self.match_(Semicolon) {
      continue
    }
    // Index signature: `[name: KeyType]: ValueType`. Detect before any
    // modifier consumption so the shape isn't lost when the body opens
    // with `[`. Stale-pattern bail-out: revert to the original skip
    // path if the syntax doesn't match.
    if self.check(LBracket) && self.is_index_signature_shape_after_lbracket() {
      match self.try_parse_class_index_signature() {
        Some(sig) => {
          index_signatures.push((sig.0, sig.1))
          continue
        }
        None => ()
      }
    }
    let mut is_static = false
    let mut is_readonly = false
    let mut is_public = true
    let mut visibility = "public"
    let mut is_accessor = false
    let mut is_abstract_member = false
    let mut saw_override = false
    let mut has_modifier = true
    while has_modifier {
      has_modifier = false
      match self.peek().kind {
        Ident(mod) if mod == "static" ||
          mod == "readonly" ||
          mod == "override" ||
          mod == "abstract" ||
          mod == "declare" ||
          mod == "accessor" ||
          mod == "public" ||
          mod == "private" ||
          mod == "protected" => {
          // A modifier-like keyword followed directly by `:` / `?:` / `(`
          // / `;` is actually being used as a class member name (TS allows
          // reserved words as property names). Stop consuming modifiers
          // so the regular member-name path picks it up. Drizzle uses
          // this for `protected: TValue` brand fields.
          match self.peek_at(1).kind {
            Colon | LParen | Semicolon | Comma | Eq => break
            Question =>
              match self.peek_at(2).kind {
                Colon | LParen | Semicolon | Comma | Eq => break
                _ => ()
              }
            _ => ()
          }
          let _ = self.advance()
          if mod == "static" {
            is_static = true
          }
          if mod == "readonly" {
            is_readonly = true
          }
          if mod == "accessor" {
            is_accessor = true
          }
          if mod == "private" || mod == "protected" {
            is_public = false
            visibility = mod
          }
          if mod == "abstract" {
            is_abstract_member = true
          }
          if mod == "override" {
            saw_override = true
          }
          has_modifier = true
        }
        _ => ()
      }
    }
    // Static initialization block: `static { ... }`. Captured so the
    // checker's definite-assignment pass can see assignments to outer
    // variables (these blocks run at class-definition time).
    if is_static && self.check(LBrace) {
      static_blocks.push(self.parse_block())
      continue
    }
    // Index signature written after modifiers (`static [index: string]:
    // string`): the pre-modifier check at the loop head can't see it.
    if self.check(LBracket) && self.is_index_signature_shape_after_lbracket() {
      match self.try_parse_class_index_signature() {
        Some(sig) => {
          if is_static {
            static_index_signatures.push((sig.0, sig.1))
          } else {
            index_signatures.push((sig.0, sig.1))
          }
          continue
        }
        None => ()
      }
    }
    let mut accessor_kind : String? = None
    match self.peek().kind {
      Ident(n) if (n == "get" || n == "set") &&
        self.peek_at(1).kind != LParen &&
        self.peek_at(1).kind != Lt &&
        self.peek_at(1).kind != Colon => {
        let _ = self.advance()
        accessor_kind = Some(n)
      }
      _ => ()
    }
    let member_name = self.parse_declare_class_member_name()
    match member_name {
      Some(n) => {
        if visibility == "private" {
          private_members.push(n)
        } else if visibility == "protected" {
          protected_members.push(n)
        }
        if is_abstract_member {
          abstract_members.push(n)
        }
        if saw_override {
          declare_override_names.push((if is_static { "s:" } else { "i:" }) + n)
        }
      }
      None => ()
    }
    let member_is_optional = self.match_(Question)
    // Definite-assignment assertion (`foo!: T`) on a class field. Also
    // tolerate a stray `!` after a method name even though that is not
    // standard syntax, mirroring how the runtime class parser handles it.
    let has_definite_assertion = self.match_(Bang)
    let mut local_type_param_bound_len = self.local_type_param_bounds.length()
    let mut method_type_params : Array[String] = []
    let mut method_type_param_bounds : Array[(String, @ast.TsType)] = []
    if self.check(Lt) {
      let (names, type_param_bounds) = self.parse_type_param_names_and_bounds()
      method_type_params = names
      method_type_param_bounds = type_param_bounds
      local_type_param_bound_len = self.push_local_type_param_bounds(
        type_param_bounds,
      )
    }
    if self.check(LParen) {
      // TS2386, fourth site. `declare class` has its own member parser, so
      // the rule written for the runtime class body, the interface and the
      // object type literal was missing from exactly the declaration kind
      // a `.d.ts` is made of. Every member here is bodiless, so unlike the
      // runtime class site there is no implementation to record as
      // required.
      match member_name {
        Some(n) if accessor_kind is None && n != "constructor" =>
          self.record_overload_optionality(
            declare_method_sig_optional,
            (if is_static { "s:" } else { "i:" }) + n,
            n,
            member_is_optional,
          )
        _ => ()
      }
      let params = self.parse_declare_signature_params()
      let had_return_annotation = self.check(Colon)
      // TS7010, fourth site: every `declare class` member is bodiless, so
      // a missing return annotation always means an implicit `any`.
      match member_name {
        Some(n) if accessor_kind is None && n != "constructor" =>
          self.record_implicit_any_member_return(n, had_return_annotation)
        _ => ()
      }
      let return_type = if had_return_annotation {
        let _ = self.advance()
        self.parse_type()
      } else {
        @ast.TsType::Any
      }
      self.restore_local_type_param_bounds(local_type_param_bound_len)
      self.consume_semicolon()
      match member_name {
        Some(member_name) =>
          match accessor_kind {
            Some("get") if is_public => {
              upsert_class_property(properties, {
                name: member_name,
                type_: return_type,
                is_static,
                is_readonly: true,
                is_accessor: false,
                has_initializer: false,
                has_definite_assertion: false,
              })
              record_ambient_accessor_method(
                methods,
                member_name,
                "get",
                is_static,
                [],
                return_type,
              )
            }
            Some("set") if is_public => {
              let setter_type = if params.length() > 0 {
                let (_, type_) = params[0]
                type_
              } else {
                @ast.TsType::Any
              }
              upsert_class_property(properties, {
                name: member_name,
                type_: setter_type,
                is_static,
                is_readonly: false,
                is_accessor: false,
                has_initializer: false,
                has_definite_assertion: false,
              })
              record_ambient_accessor_method(
                methods,
                member_name,
                "set",
                is_static,
                params,
                @ast.TsType::Void,
              )
            }
            _ =>
              if member_name == "constructor" &&
                !is_static &&
                accessor_kind is None {
                if is_public {
                  constructor_params = Some(params)
                  saw_public_constructor = true
                } else {
                  saw_nonpublic_constructor = true
                }
              } else if is_public {
                methods.push({
                  name: member_name,
                  type_params: method_type_params,
                  type_param_bounds: method_type_param_bounds,
                  params,
                  return_type,
                  is_static,
                  // Ambient class declarations (`declare class`) and
                  // interface-style method signatures don't carry a
                  // body — leave it as `None`.
                  body: None,
                  accessor: "",
                  key_expr: None,
                  is_async: false,
                  is_generator: false,
                  param_defaults: [],
                  param_is_rest: [],
                })
              }
          }
        None => ()
      }
      continue
    }
    self.restore_local_type_param_bounds(local_type_param_bound_len)
    let mut property_type = @ast.TsType::Any
    if self.match_(Colon) {
      property_type = self.parse_type()
    }
    let has_initializer = if self.match_(Eq) {
      let init_expr = self.parse_assignment()
      // Ambient classes carry no runtime initializer, but tsc still
      // types the legal `static readonly X = <literal>` form from its
      // literal. Mirror that instead of leaving the member `any`.
      if property_type is Any {
        match ambient_literal_property_type(init_expr, is_readonly) {
          Some(inferred) => property_type = inferred
          None => ()
        }
      }
      true
    } else {
      false
    }
    self.consume_semicolon()
    match member_name {
      Some(member_name) if is_public && accessor_kind is None =>
        upsert_class_property(properties, {
          name: member_name,
          type_: property_type,
          is_static,
          is_readonly,
          is_accessor,
          has_initializer,
          has_definite_assertion,
        })
      _ => ()
    }
  }
  let _ = self.expect(RBrace)
  let _ = self.match_(Semicolon)
  let is_constructible = if saw_public_constructor {
    !is_abstract
  } else if saw_nonpublic_constructor {
    false
  } else {
    !is_abstract
  }
  self.restore_local_type_param_bounds(class_type_param_bound_len)
  {
    name,
    type_params: class_type_params,
    type_param_bounds: class_type_param_bounds,
    computed_field_keys: [],
    base_names,
    implements_names,
    base_expr: None,
    static_field_inits: [],
    static_blocks,
    instance_field_inits: [],
    constructor_params,
    // The ambient `declare class` param parser does not surface rest flags;
    // leave empty (rest detection just no-ops for these).
    constructor_param_is_rest: [],
    constructor_param_defaults: [],
    constructor_body: None,
    properties,
    methods,
    index_signatures,
    static_index_signatures,
    is_abstract,
    is_constructible,
    is_local: false,
    decorators: self.take_pending_decorators(),
    is_declare: true,
    private_members,
    protected_members,
    abstract_members,
    duplicate_member_names: {
      let d : Array[String] = []
      for n in declare_override_names {
        d.push("<override-member:" + n + ">")
      }
      d
    },
  }
}

///|
/// Literal type of an ambient class property initializer
/// (`declare class C { static readonly X = "lit" }`). tsc types the
/// readonly form as the literal itself and the (illegal-but-tolerated)
/// non-readonly form as the widened primitive. Restricted to literals
/// whose type reconstruction is exact; anything else abstains.
fn ambient_literal_property_type(
  expr : @ast.TsExpr,
  is_readonly : Bool,
) -> @ast.TsType? {
  match expr {
    @ast.TsExpr::StringLit(value) =>
      Some(
        if is_readonly {
          @ast.TsType::Literal(value)
        } else {
          @ast.TsType::String_
        },
      )
    @ast.TsExpr::BoolLit(value) =>
      Some(
        if is_readonly {
          @ast.TsType::BooleanLiteral(value)
        } else {
          @ast.TsType::Boolean
        },
      )
    @ast.TsExpr::IntLit(value) =>
      Some(
        if is_readonly {
          @ast.TsType::NumberLiteral(value.to_string())
        } else {
          @ast.TsType::Int
        },
      )
    _ => None
  }
}
